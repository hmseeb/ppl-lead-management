---
phase: 39-call-reporting-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/portal/call-queries.ts
  - src/components/portal/call-kpi-cards.tsx
  - src/components/portal/call-outcome-chart.tsx
autonomous: true
requirements: [CALL-01, CALL-02, CALL-03, CALL-05, CALL-06]

must_haves:
  truths:
    - "Broker-scoped call KPI data is queryable by broker_id and date range"
    - "Broker-scoped call outcome volume data returns bucketed daily/weekly data"
    - "Portal KPI cards display total calls, transferred, callbacks booked, no answer, voicemail with percentages"
    - "Portal outcome chart renders a stacked bar chart with recharts"
  artifacts:
    - path: "src/lib/portal/call-queries.ts"
      provides: "Broker-scoped call reporting queries"
      exports: ["fetchPortalCallKpis", "fetchPortalCallOutcomeVolume", "fetchPortalUpcomingCallbacks"]
    - path: "src/components/portal/call-kpi-cards.tsx"
      provides: "Client-facing KPI cards for call metrics"
      exports: ["PortalCallKpiCards"]
    - path: "src/components/portal/call-outcome-chart.tsx"
      provides: "Client-facing stacked bar chart for call outcomes"
      exports: ["PortalCallOutcomeChart"]
  key_links:
    - from: "src/lib/portal/call-queries.ts"
      to: "call_logs table"
      via: "createAdminClient with broker_id filter"
      pattern: "eq\\('broker_id'"
    - from: "src/lib/portal/call-queries.ts"
      to: "src/lib/types/portal-filters.ts"
      via: "getPortalDateRange import"
      pattern: "getPortalDateRange"
    - from: "src/components/portal/call-outcome-chart.tsx"
      to: "recharts"
      via: "BarChart with stacked bars"
      pattern: "stackId"
---

<objective>
Create broker-scoped call reporting queries and client-facing display components (KPI cards + outcome chart) for the portal calls page.

Purpose: Provides the data layer and visual components that the /portal/calls page will assemble. Queries mirror admin call-reporting.ts but are broker-scoped via broker_id filter and use portal date filter types.

Output: Three files -- portal call queries, portal KPI cards component, portal outcome chart component.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-portal-date-range-filters/38-01-SUMMARY.md
@src/lib/queries/call-reporting.ts
@src/lib/portal/queries.ts
@src/lib/types/portal-filters.ts
@src/components/calls/call-kpi-cards.tsx
@src/components/calls/call-outcome-chart.tsx
@src/components/portal/spend-trend-chart.tsx
@src/components/portal/dashboard-cards.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create broker-scoped portal call queries</name>
  <files>src/lib/portal/call-queries.ts</files>
  <action>
Create a new file src/lib/portal/call-queries.ts with three broker-scoped query functions. Follow the exact pattern from src/lib/portal/queries.ts (createAdminClient, explicit broker_id filter, optional PortalDateFilters param).

1. **fetchPortalCallKpis(brokerId: string, dateFilters?: PortalDateFilters)** returning PortalCallKpis type:
   - Type: `{ totalCalls: number, transferred: number, callbacksBooked: number, noAnswer: number, voicemail: number }`
   - Query call_logs table with `.eq('broker_id', brokerId)` and date range from `getPortalDateRange(dateFilters)` applied to `created_at`
   - Use the same Promise.all pattern as admin fetchCallKpis with `.select('id', { count: 'exact', head: true })` for each outcome
   - When dateFilters is undefined, default to 30d via getPortalDateRange({}) behavior

2. **fetchPortalCallOutcomeVolume(brokerId: string, dateFilters?: PortalDateFilters)** returning PortalCallOutcomeVolume type:
   - Type: `{ data: Array<{ date: string, label: string, transferred: number, callback_booked: number, no_answer: number, voicemail: number }>, bucketType: 'daily' | 'weekly', totalDays: number }`
   - Mirror admin fetchCallOutcomeVolume logic: fetch all call_logs in range, bucket in TypeScript, use daily for <=30 days else weekly
   - Always filter by broker_id
   - Use date-fns (startOfDay, addDays, format, differenceInDays) same as admin version

3. **fetchPortalUpcomingCallbacks(brokerId: string, limit?: number)** returning PortalUpcomingCallback[] type:
   - Type: `{ id: string, scheduled_time: string, status: string, lead: { first_name: string | null, last_name: string | null } | null }`
   - Query callbacks table with `.eq('broker_id', brokerId)`, `.eq('status', 'pending')`, `.gte('scheduled_time', new Date().toISOString())`
   - Join leads table for lead name: `.select('id, scheduled_time, status, leads ( first_name, last_name )')`
   - No broker join needed (data is broker-scoped, no broker column in output)
   - Order by scheduled_time ascending, limit defaults to 10
   - Map results to flatten the leads join

Import createAdminClient from '@/lib/supabase/admin', import getPortalDateRange and PortalDateFilters from '@/lib/types/portal-filters', import date-fns functions.

Export all types and functions.
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && npx tsc --noEmit src/lib/portal/call-queries.ts 2>&1 | head -20</automated>
    <manual>Verify the file exports three functions and three types, all broker-scoped</manual>
  </verify>
  <done>Three broker-scoped call query functions exist with proper types, broker_id filtering, and optional date range support</done>
</task>

<task type="auto">
  <name>Task 2: Create portal call KPI cards and outcome chart components</name>
  <files>src/components/portal/call-kpi-cards.tsx, src/components/portal/call-outcome-chart.tsx</files>
  <action>
**src/components/portal/call-kpi-cards.tsx** -- Create a client-facing KPI cards component. This is NOT a copy of the admin version. Key differences:
- No click-to-expand drill-down (admin feature, not appropriate for client portal)
- Static, polished cards with clean typography
- No cursor-pointer, no onClick handlers, no expandedCard state, no preview data fetching

Implementation:
- Export `PortalCallKpiCards` component accepting `{ data: PortalCallKpis }` from the new call-queries types
- Render a grid of 5 cards: Total Calls, Transferred, Callbacks Booked, No Answer, Voicemail
- Use `grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-4` layout
- Each card: Card with CardHeader (title + icon) and CardContent (value + percentage)
- Icons: Phone (blue-500), PhoneForwarded (emerald-500), CalendarClock (violet-500), PhoneMissed (amber-500), Voicemail (rose-500) from lucide-react
- Title: `text-[11px] font-medium uppercase tracking-wider text-muted-foreground`
- Value: `text-2xl font-semibold tabular-nums` (slightly smaller than admin's 3xl, more refined)
- Percentage line: For transferred/callbacksBooked/noAnswer/voicemail, show `{percent}% of total` in `text-xs text-muted-foreground`. For Total Calls, show "All outcomes"
- Calculate percentage as `Math.round((value / data.totalCalls) * 100)`, guard against totalCalls === 0
- Use subtle background tints matching icon color: e.g., `bg-blue-500/5` for Total Calls card
- No glow effects or hover:scale transforms (keep it clean and professional)

**src/components/portal/call-outcome-chart.tsx** -- Create a client-facing stacked bar chart. Follow the pattern from src/components/portal/spend-trend-chart.tsx for styling consistency.

Implementation:
- Export `PortalCallOutcomeChart` component accepting `{ data: PortalCallOutcomeVolume['data'], bucketType?: 'daily' | 'weekly', totalDays?: number }`
- Import PortalCallOutcomeVolume type from the new call-queries
- 'use client' directive (recharts requires client rendering)
- Use useTheme from next-themes for dark mode support
- Render recharts BarChart inside Card with CardHeader (BarChart3 icon from lucide + dynamic title) and CardContent
- Dynamic title: "Call Outcomes (Today)" / "Call Outcomes (7 Days)" / "Call Outcomes (30 Days)" etc, same logic as admin getChartTitle
- Chart height: `h-[250px]` in ResponsiveContainer
- Stacked bars with stackId="outcome": transferred (#10b981), callback_booked (#8b5cf6), no_answer (#f59e0b), voicemail (#f43f5e)
- Top bar gets radius [4,4,0,0] for rounded tops
- XAxis: dataKey="label", fontSize 10, no tickLine/axisLine
- YAxis: fontSize 10, no tickLine/axisLine, allowDecimals false
- Tooltip styling: match spend-trend-chart.tsx pattern (rounded-8, monospace font, dark/light backgrounds, subtle colored border)
- Legend: wrapperStyle fontSize 10, paddingTop 8
- Bar names: "Transferred", "Callback Booked", "No Answer", "Voicemail"
- Handle empty data: show "No call data yet." text when data is empty or all zeros
- Use the same color values as admin chart for consistency across the app

Both components are 'use client' only if they need hooks. KPI cards can be a server component if no hooks are used. The outcome chart MUST be 'use client' because recharts and useTheme.

For the KPI cards, since there are no interactive hooks needed, make it a regular component (no 'use client'). It receives data as props from the server page.
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && npx tsc --noEmit src/components/portal/call-kpi-cards.tsx src/components/portal/call-outcome-chart.tsx 2>&1 | head -20</automated>
    <manual>Verify KPI cards have no click handlers, chart uses recharts stacked bars</manual>
  </verify>
  <done>Portal KPI cards render 5 static cards with percentages, outcome chart renders stacked bar chart with proper dark/light theme support</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors on all three new files
- src/lib/portal/call-queries.ts exports fetchPortalCallKpis, fetchPortalCallOutcomeVolume, fetchPortalUpcomingCallbacks
- src/components/portal/call-kpi-cards.tsx exports PortalCallKpiCards (no click-to-expand)
- src/components/portal/call-outcome-chart.tsx exports PortalCallOutcomeChart (recharts stacked BarChart)
</verification>

<success_criteria>
- Three broker-scoped query functions exist, each filtering by broker_id
- KPI cards show 5 metrics with percentage-of-total indicators, no drill-down
- Outcome chart renders stacked bars with daily/weekly bucketing
- All components follow portal design language (polished, not admin copy)
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/39-call-reporting-page/39-01-SUMMARY.md`
</output>
