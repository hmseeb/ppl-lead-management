---
phase: 08-delivery-stats-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/queries/dashboard.ts
  - src/components/dashboard/delivery-stats-cards.tsx
  - src/app/(dashboard)/page.tsx
autonomous: true
requirements:
  - MNTR-01
  - MNTR-02
  - MNTR-03
  - MNTR-05

must_haves:
  truths:
    - "Dashboard shows today's lead counts: received, assigned, unassigned"
    - "Dashboard shows today's delivery counts by channel (webhook, email, SMS)"
    - "Dashboard shows today's failed delivery count with per-channel breakdown"
    - "Each channel shows a color-coded health indicator (green/yellow/red/gray)"
  artifacts:
    - path: "src/lib/queries/dashboard.ts"
      provides: "fetchDeliveryStats() returning today's delivery counts by channel and status"
      contains: "fetchDeliveryStats"
    - path: "src/components/dashboard/delivery-stats-cards.tsx"
      provides: "Delivery Health section with KPI cards and channel health indicators"
      contains: "DeliveryStatsCards"
    - path: "src/app/(dashboard)/page.tsx"
      provides: "Dashboard page calling fetchDeliveryStats and rendering DeliveryStatsCards"
      contains: "fetchDeliveryStats"
  key_links:
    - from: "src/app/(dashboard)/page.tsx"
      to: "src/lib/queries/dashboard.ts"
      via: "fetchDeliveryStats() in Promise.all"
      pattern: "fetchDeliveryStats"
    - from: "src/app/(dashboard)/page.tsx"
      to: "src/components/dashboard/delivery-stats-cards.tsx"
      via: "DeliveryStatsCards component with data prop"
      pattern: "DeliveryStatsCards"
    - from: "src/lib/queries/dashboard.ts"
      to: "supabase deliveries table"
      via: "count queries with created_at >= todayStart"
      pattern: "from.*deliveries.*count.*exact.*head.*true"
---

<objective>
Add delivery stats query function, Delivery Health UI section with channel breakdown and health indicators, and wire into the existing dashboard page.

Purpose: Admin can see today's delivery health at a glance without navigating to individual lead or broker pages. Color-coded health indicators surface problems immediately.
Output: fetchDeliveryStats() function, DeliveryStatsCards component, updated dashboard page.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-delivery-stats-dashboard/08-RESEARCH.md

@src/lib/queries/dashboard.ts
@src/components/dashboard/kpi-cards.tsx
@src/app/(dashboard)/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fetchDeliveryStats() query function</name>
  <files>src/lib/queries/dashboard.ts</files>
  <action>
Add a new exported async function `fetchDeliveryStats()` to the existing dashboard.ts query file. Follow the exact same pattern as `fetchKpis()`: use `createAdminClient()`, `startOfDay(new Date()).toISOString()` for todayStart, and parallel count queries with `{ count: 'exact', head: true }`.

The function runs 9 parallel count queries against the `deliveries` table, all filtered by `gte('created_at', todayStart)`:
1. Total deliveries today
2. Sent deliveries today (status = 'sent')
3. Failed deliveries today (status IN ['failed', 'failed_permanent'])
4. Per-channel totals: crm_webhook total, email total, sms total
5. Per-channel failures: crm_webhook failed, email failed, sms failed

Also add 3 more parallel count queries against the `leads` table for MNTR-01 today-scoped lead stats:
6. Leads received today: gte('created_at', todayStart)
7. Leads assigned today: gte('created_at', todayStart) AND eq('status', 'assigned')
8. Leads unassigned today: gte('created_at', todayStart) AND eq('status', 'unassigned')

Return shape:
```typescript
{
  leads: { received: number, assigned: number, unassigned: number },
  total: number,
  sent: number,
  failed: number,
  channels: {
    crm_webhook: { total: number, failed: number },
    email: { total: number, failed: number },
    sms: { total: number, failed: number },
  }
}
```

Export the return type as `DeliveryStats` for the component to import.

Use `?? 0` for all count values (matching existing pattern). Do NOT create a SQL view. Do NOT modify existing functions.
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Verify fetchDeliveryStats is exported and return type matches spec</manual>
  </verify>
  <done>fetchDeliveryStats() is exported from dashboard.ts, returns DeliveryStats type with leads (received/assigned/unassigned), total, sent, failed, and per-channel breakdown. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create DeliveryStatsCards component with channel health indicators</name>
  <files>src/components/dashboard/delivery-stats-cards.tsx</files>
  <action>
Create a new component file that renders a "Delivery Health" section below the existing KPI cards. Follow the EXACT styling pattern from kpi-cards.tsx (Card, CardHeader, CardTitle, CardContent, icon sizing, font sizing, glow effects).

**Component structure:**

1. Section header: "Delivery Health" with a horizontal rule, matching the "Overview" header style but smaller (text-sm font-medium text-muted-foreground with a subtle left border or top divider).

2. Top row: 4 KPI cards in a responsive grid (`grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4`):
   - "Leads Today" showing leads.received with subtitle "{leads.assigned} assigned, {leads.unassigned} unassigned". Icon: Inbox from lucide-react, color: text-blue-400, glow: blue.
   - "Deliveries" showing total with subtitle "{sent} sent today". Icon: Send from lucide-react, color: text-emerald-400, glow: green.
   - "Failed" showing failed with subtitle "across all channels". Icon: AlertTriangle from lucide-react, color conditional: text-red-400 if failed > 0, text-muted-foreground if 0. Glow: red if failed > 0, none if 0. Highlight border (!border-red-500/20) if failed > 0.
   - "Success Rate" showing percentage as `total > 0 ? Math.round(((total - failed) / total) * 100) : 100` with "%" suffix. Icon: CheckCircle from lucide-react. Color conditional: text-emerald-400 if >= 90%, text-amber-400 if >= 75%, text-red-400 if < 75%.

3. Bottom row: 3 channel cards in `grid-cols-1 md:grid-cols-3 gap-4` showing per-channel breakdown:
   - Each card shows channel name (display: "Webhook", "Email", "SMS"), total count, failed count, and a health indicator dot.
   - Health indicator: a small colored circle (w-2 h-2 rounded-full inline-block) next to the channel title in the CardHeader.
   - Health logic: `getChannelHealth(total, failed)` function:
     - total === 0: 'inactive' -> bg-muted-foreground/50, label "No data"
     - failed === 0: 'healthy' -> bg-emerald-400, label "Healthy"
     - failed/total < 0.25: 'degraded' -> bg-amber-400, label "Degraded"
     - failed/total >= 0.25: 'critical' -> bg-red-400, label "Critical"
   - Card subtitle shows the health label text.
   - Icons per channel: Globe for webhook, Mail for email, MessageSquare for sms (all from lucide-react).

Import `DeliveryStats` type from `@/lib/queries/dashboard`. The component receives `{ data: DeliveryStats }` as props.

Use the same Card/CardHeader/CardTitle/CardContent imports as kpi-cards.tsx. Match the exact text sizes (text-[11px] for title, text-3xl font-bold font-mono for value, text-[10px] for subtitle).
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Verify component renders cards matching kpi-cards.tsx pattern with health indicator dots</manual>
  </verify>
  <done>DeliveryStatsCards component exists with 4 top KPI cards (leads today, deliveries, failed, success rate) and 3 channel breakdown cards (webhook, email, SMS) each with color-coded health indicator. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Wire DeliveryStatsCards into dashboard page</name>
  <files>src/app/(dashboard)/page.tsx</files>
  <action>
Update the dashboard page to:

1. Import `fetchDeliveryStats` from `@/lib/queries/dashboard`
2. Import `DeliveryStatsCards` from `@/components/dashboard/delivery-stats-cards`
3. Add `fetchDeliveryStats()` to the existing `Promise.all` call (4th item alongside fetchKpis, fetchRecentActivity, fetchLeadVolume7Days)
4. Destructure the 4th result as `deliveryStats`
5. Render `<DeliveryStatsCards data={deliveryStats} />` between `<KpiCards>` and the chart/activity grid

The resulting JSX order should be:
```
<h1>Overview</h1>
<KpiCards />
<DeliveryStatsCards />  <!-- NEW -->
<div grid> LeadVolumeChart + ActivityFeed </div>
```

Do NOT modify any existing component rendering or props. Only add the new import, Promise.all entry, and JSX element.
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Run `bun dev` and visit localhost:3000 to see Delivery Health section below KPI cards</manual>
  </verify>
  <done>Dashboard page fetches delivery stats in parallel with existing queries and renders DeliveryStatsCards between KPI cards and the chart/activity grid. TypeScript compiles. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `bun run build` completes successfully
3. Dashboard page loads without error and shows both KPI and Delivery Health sections
4. Delivery Health section shows 7 cards: 4 top row (leads, deliveries, failed, success rate) + 3 bottom row (webhook, email, SMS channels)
5. Channel health dots show correct colors based on failure rates
6. Zero-data state shows "0" values with muted styling and gray health indicators
</verification>

<success_criteria>
- fetchDeliveryStats() returns today-scoped lead and delivery counts by channel
- DeliveryStatsCards renders with matching KPI card styling
- Channel health indicators show green/yellow/red/gray based on failure rate thresholds
- Dashboard page loads all data in parallel (no waterfall)
- Empty state handled gracefully (0 counts, gray health dots, 100% success rate)
</success_criteria>

<output>
After completion, create `.planning/phases/08-delivery-stats-dashboard/08-01-SUMMARY.md`
</output>
