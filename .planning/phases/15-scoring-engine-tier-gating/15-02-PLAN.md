---
phase: 15-scoring-engine-tier-gating
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - supabase/migrations/00021_scoring_assign_lead.sql
  - src/lib/types/database.ts
  - src/lib/assignment/assign.ts
  - src/app/api/leads/incoming/route.ts
autonomous: true
requirements:
  - SCORE-01
  - TIER-01
  - TIER-03
  - ORDER-05

must_haves:
  truths:
    - "A lead arriving via webhook is scored using the TypeScript scoring engine instead of the SQL ORDER BY rotation"
    - "The winning order from scoring is passed to the SQL function which handles atomic assignment (advisory lock, counter updates, delivery creation)"
    - "When no order passes hard filters, the lead is marked unassigned with a descriptive reason"
    - "The existing advisory lock serialization and delivery creation flow is preserved"
    - "A 680+ lead routes to a 680-min order first, falling back to 600-min only when no 680-min eligible"
  artifacts:
    - path: "supabase/migrations/00021_scoring_assign_lead.sql"
      provides: "Updated assign_lead that accepts order_id parameter and skips the ORDER BY query"
      contains: "p_order_id"
    - path: "src/lib/assignment/assign.ts"
      provides: "Refactored assignLead that calls scoreLead then passes winner to SQL"
      exports: ["assignLead", "AssignmentResult"]
      min_lines: 30
    - path: "src/app/api/leads/incoming/route.ts"
      provides: "Webhook route passing lead data to assignLead (which now uses scoring internally)"
  key_links:
    - from: "src/lib/assignment/assign.ts"
      to: "src/lib/assignment/scoring.ts"
      via: "import { scoreLead }"
      pattern: "scoreLead"
    - from: "src/lib/assignment/assign.ts"
      to: "supabase rpc assign_lead"
      via: "supabase.rpc('assign_lead', { p_lead_id, p_order_id })"
      pattern: "rpc.*assign_lead"
    - from: "src/app/api/leads/incoming/route.ts"
      to: "src/lib/assignment/assign.ts"
      via: "import { assignLead }"
      pattern: "assignLead"
---

<objective>
Wire the TypeScript scoring engine into the assignment flow, replacing the SQL ORDER BY weighted rotation.

Purpose: The scoring engine (Plan 01) produces the winning order. This plan refactors the SQL `assign_lead()` function to accept the pre-computed winner, and updates the TypeScript `assignLead()` wrapper to orchestrate: fetch eligible orders -> score via TypeScript -> pass winner to SQL for atomic assignment.

Output: Updated migration, refactored assign.ts, and the webhook route now uses scoring-based assignment.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-scoring-engine-tier-gating/15-01-SUMMARY.md
@src/lib/assignment/assign.ts
@src/lib/assignment/scoring.ts
@src/lib/types/database.ts
@src/app/api/leads/incoming/route.ts
@src/lib/delivery/dispatcher.ts
@supabase/migrations/00016_hours_aware_delivery.sql (current assign_lead SQL function)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor assign_lead SQL to accept order_id parameter</name>
  <files>supabase/migrations/00021_scoring_assign_lead.sql, src/lib/types/database.ts</files>
  <action>
    Create migration `00021_scoring_assign_lead.sql` that replaces the `assign_lead()` function with a new version accepting TWO parameters: `p_lead_id uuid` and `p_order_id uuid DEFAULT NULL`.

    When `p_order_id` is provided (the scoring path):
    - Still acquire advisory lock: `PERFORM pg_advisory_xact_lock(1, 0);`
    - Fetch the lead (same as before)
    - Fetch the order by p_order_id directly: `SELECT * INTO v_order FROM orders WHERE id = p_order_id AND status = 'active';`
    - If order not found (race condition: completed between scoring and lock), fall back to unassigned path
    - Re-verify the order still has capacity: `leads_remaining > 0 OR bonus_mode = true` (another race condition guard)
    - If capacity gone, fall back to unassigned path
    - Proceed with existing assignment logic: update lead, update order counters, auto-complete check, activity log, delivery creation (all unchanged from migration 00016)

    When `p_order_id` is NULL (backward compatibility / unassigned path):
    - Run the OLD ORDER BY query as fallback (keeps existing behavior for edge cases)

    The unassigned path remains identical: `build_match_failure_reason()`, update lead status, insert unassigned_queue, activity_log.

    The delivery creation block (contact hours check, multi-channel inserts) stays EXACTLY as-is from migration 00016. Do not modify delivery logic.

    After migration is applied, regenerate TypeScript types:
    ```bash
    bunx supabase gen types typescript --local > src/lib/types/database.ts
    ```

    The Function type for assign_lead should now show `Args: { p_lead_id: string; p_order_id?: string }`.
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && bunx supabase db reset && bunx supabase gen types typescript --local > src/lib/types/database.ts && grep -q "p_order_id" src/lib/types/database.ts && echo "PASS: assign_lead accepts p_order_id"</automated>
    <manual>Check migration file has both code paths (with and without p_order_id)</manual>
  </verify>
  <done>assign_lead() SQL function accepts optional p_order_id, skips ORDER BY query when provided, falls back to old behavior when NULL. Types regenerated.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor TypeScript assignLead to use scoring engine</name>
  <files>src/lib/assignment/assign.ts, src/app/api/leads/incoming/route.ts</files>
  <action>
    Refactor `src/lib/assignment/assign.ts` to orchestrate scoring-based assignment:

    1. Import `scoreLead` and types from `./scoring`
    2. The `assignLead(leadId)` function should now:
       a. Fetch the lead from DB (need credit_score, funding_amount, vertical)
       b. Fetch all candidate orders with joined broker data:
          ```sql
          SELECT o.*, b.assignment_status as broker_assignment_status
          FROM orders o
          JOIN brokers b ON b.id = o.broker_id
          WHERE o.status = 'active'
            AND b.assignment_status = 'active'
          ```
          (Fetch broadly. Hard filters and scoring happen in TypeScript.)
       c. Call `scoreLead(lead, orders)` to get sorted scored orders
       d. If no scored orders returned (all filtered out), call `supabase.rpc('assign_lead', { p_lead_id: leadId })` with NO p_order_id. This lets the SQL function handle the unassigned path with its existing `build_match_failure_reason()` logic.
       e. If scored orders returned, call `supabase.rpc('assign_lead', { p_lead_id: leadId, p_order_id: scoredOrders[0].order_id })` to assign the winner.
       f. Return the same `AssignmentResult` shape as before.

    The `AssignmentResult` interface stays the same. The webhook route (`src/app/api/leads/incoming/route.ts`) should NOT need changes since it already calls `assignLead(lead.id)` and handles the result. Verify the route still works as-is. If the route references the old return shape, adjust minimally.

    Important: The Supabase query for orders needs to select all fields the scoring engine needs: id, broker_id, status, verticals, credit_score_min, leads_remaining, leads_delivered, total_leads, bonus_mode, loan_min, loan_max, priority, and the joined broker_assignment_status. Use `.select('*, brokers!inner(assignment_status)')` or a raw query approach that works with the generated types.

    Since Supabase JS join syntax can be finicky with generated types, consider using a typed query helper or casting as needed. The goal is type-safe data flowing into `scoreLead()`.
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && bun run build 2>&1 | tail -20</automated>
    <manual>Review that assignLead fetches orders, calls scoreLead, passes winner to SQL RPC</manual>
  </verify>
  <done>assignLead() fetches eligible orders, scores them via scoreLead(), passes the winning order_id to the SQL function for atomic assignment. Build succeeds with no type errors. The webhook route continues to work without modification.</done>
</task>

</tasks>

<verification>
```bash
# 1. Scoring tests still pass
cd /Users/haseeb/ppl-leadr-mgmt && bun test src/lib/assignment/scoring.test.ts

# 2. Build succeeds (no type errors)
cd /Users/haseeb/ppl-leadr-mgmt && bun run build 2>&1 | tail -5

# 3. SQL function exists with new signature
cd /Users/haseeb/ppl-leadr-mgmt && bunx supabase db reset && echo "SELECT proname, pronargs FROM pg_proc WHERE proname = 'assign_lead';" | bunx supabase db query
```
</verification>

<success_criteria>
- assign_lead SQL function accepts optional p_order_id parameter
- TypeScript assignLead() calls scoreLead() to determine winner
- Winner's order_id is passed to SQL for atomic assignment
- When scoring returns no eligible orders, SQL handles unassigned path
- Advisory lock serialization preserved (race condition safe)
- Delivery creation flow unchanged
- `bun run build` succeeds
- `bun test scoring.test.ts` still passes
</success_criteria>

<output>
After completion, create `.planning/phases/15-scoring-engine-tier-gating/15-02-SUMMARY.md`
</output>
