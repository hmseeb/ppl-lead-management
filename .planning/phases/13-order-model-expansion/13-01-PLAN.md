---
phase: 13-order-model-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00019_order_model_expansion.sql
  - src/lib/types/database.ts
  - src/lib/schemas/order.ts
  - src/lib/actions/orders.ts
autonomous: true
requirements:
  - ORDER-01
  - ORDER-02

must_haves:
  truths:
    - "Orders table has loan_min and loan_max integer columns (nullable)"
    - "Orders table has priority column defaulting to 'normal'"
    - "Orders table has order_type column defaulting to 'one_time'"
    - "Zod schema validates loan_min <= loan_max when both provided"
    - "createOrder server action persists all new fields to database"
  artifacts:
    - path: "supabase/migrations/00019_order_model_expansion.sql"
      provides: "ALTER TABLE adding loan_min, loan_max, priority, order_type columns"
      contains: "ALTER TABLE orders"
    - path: "src/lib/types/database.ts"
      provides: "Updated TypeScript types with new order columns"
      contains: "loan_min"
    - path: "src/lib/schemas/order.ts"
      provides: "Zod validation for all order fields including new ones"
      contains: "loan_min"
    - path: "src/lib/actions/orders.ts"
      provides: "createOrder action that inserts loan_min, loan_max, priority, order_type"
      contains: "loan_min"
  key_links:
    - from: "src/lib/schemas/order.ts"
      to: "src/lib/actions/orders.ts"
      via: "orderSchema import and safeParse"
      pattern: "orderSchema.safeParse"
    - from: "src/lib/actions/orders.ts"
      to: "supabase orders table"
      via: "supabase.from('orders').insert()"
      pattern: "loan_min.*loan_max.*priority.*order_type"
---

<objective>
Add loan range, priority, and order type columns to the orders database table, update TypeScript types, Zod validation schema, and server action to support the new fields.

Purpose: The scoring engine (Phase 15) needs loan_min/loan_max for loan fit scoring, priority for bonus points, and order_type to distinguish one-time from monthly recurring orders. This plan lays the data foundation.
Output: Migration file, updated types, updated schema, updated createOrder action.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@supabase/migrations/00003_create_tables.sql
@src/lib/types/database.ts
@src/lib/schemas/order.ts
@src/lib/actions/orders.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration and regenerate TypeScript types</name>
  <files>supabase/migrations/00019_order_model_expansion.sql, src/lib/types/database.ts</files>
  <action>
Create migration `supabase/migrations/00019_order_model_expansion.sql` with:

1. `ALTER TABLE orders ADD COLUMN loan_min integer;` (nullable, no default)
2. `ALTER TABLE orders ADD COLUMN loan_max integer;` (nullable, no default)
3. `ALTER TABLE orders ADD COLUMN priority text NOT NULL DEFAULT 'normal' CHECK (priority IN ('high', 'normal'));`
4. `ALTER TABLE orders ADD COLUMN order_type text NOT NULL DEFAULT 'one_time' CHECK (order_type IN ('one_time', 'monthly'));`
5. Add a CHECK constraint: `ALTER TABLE orders ADD CONSTRAINT loan_range_check CHECK (loan_min IS NULL OR loan_max IS NULL OR loan_min <= loan_max);`

Use text CHECK constraints (not enum types) per project convention (see Phase 1 decision: "Used text CHECK constraints instead of enum types for status columns").

After creating the migration, apply it to the live database:
```bash
bunx supabase db push --linked
```

Then regenerate TypeScript types:
```bash
bunx supabase gen types typescript --linked > src/lib/types/database.ts
```

Verify the generated types include loan_min, loan_max, priority, and order_type on the orders Row/Insert/Update types.
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && grep -q "loan_min" src/lib/types/database.ts && grep -q "loan_max" src/lib/types/database.ts && grep -q "priority" src/lib/types/database.ts && grep -q "order_type" src/lib/types/database.ts && echo "PASS: All new fields present in types"</automated>
    <manual>Confirm migration applied without errors and types regenerated</manual>
  </verify>
  <done>Migration 00019 exists and is applied. database.ts includes loan_min (number | null), loan_max (number | null), priority (string), and order_type (string) on the orders type.</done>
</task>

<task type="auto">
  <name>Task 2: Update Zod schema and createOrder server action</name>
  <files>src/lib/schemas/order.ts, src/lib/actions/orders.ts</files>
  <action>
**Update `src/lib/schemas/order.ts`:**

Add to the orderSchema zod object:
- `loan_min`: `z.union([z.coerce.number().int().min(0, 'Must be positive'), z.literal(null)])` (nullable, optional loan floor)
- `loan_max`: `z.union([z.coerce.number().int().min(0, 'Must be positive'), z.literal(null)])` (nullable, optional loan ceiling)
- `priority`: `z.enum(['high', 'normal']).default('normal')`
- `order_type`: `z.enum(['one_time', 'monthly']).default('one_time')`

Add a `.refine()` on the schema to validate that when both loan_min and loan_max are provided (not null), loan_min <= loan_max. Error message: "Loan min must be less than or equal to loan max". Path: ['loan_max'].

Export `PRIORITIES` and `ORDER_TYPES` as const arrays for use in the form:
```ts
export const PRIORITIES = ['normal', 'high'] as const
export const ORDER_TYPES = ['one_time', 'monthly'] as const
```

**Update `src/lib/actions/orders.ts`:**

In `createOrder`:
1. Destructure `loan_min`, `loan_max`, `priority`, `order_type` from `result.data`
2. Add them to the `.insert({...})` call
3. Include them in the activity_log details object

Update the defaultValues comment: the form will send these fields. The schema defaults handle priority='normal' and order_type='one_time' when not explicitly set.
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && grep -q "loan_min" src/lib/schemas/order.ts && grep -q "order_type" src/lib/schemas/order.ts && grep -q "loan_min" src/lib/actions/orders.ts && grep -q "order_type" src/lib/actions/orders.ts && echo "PASS: Schema and action updated"</automated>
    <manual>Review that refine() validates loan_min <= loan_max correctly</manual>
  </verify>
  <done>orderSchema includes loan_min, loan_max, priority, order_type with proper validation. createOrder persists all four new fields and logs them in activity_log.</done>
</task>

</tasks>

<verification>
1. Migration file exists at supabase/migrations/00019_order_model_expansion.sql
2. TypeScript types include all 4 new fields on orders Row/Insert/Update
3. Zod schema validates loan range ordering (min <= max)
4. createOrder action includes all new fields in insert and activity log
5. `bun run build` passes (no type errors)
</verification>

<success_criteria>
- Database orders table has loan_min, loan_max, priority, order_type columns with correct types and defaults
- TypeScript types are in sync with database schema
- Zod validation enforces loan_min <= loan_max when both provided
- createOrder server action persists all new fields
- No build errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-order-model-expansion/13-01-SUMMARY.md`
</output>
