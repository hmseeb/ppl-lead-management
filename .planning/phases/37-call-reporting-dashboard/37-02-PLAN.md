---
phase: 37-call-reporting-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - src/app/(dashboard)/calls/page.tsx
  - src/lib/queries/call-reporting.ts
  - src/components/calls/call-outcome-chart.tsx
  - src/components/calls/upcoming-callbacks.tsx
autonomous: true
requirements: [RPT-02, RPT-04]

must_haves:
  truths:
    - "Admin sees a bar chart of call outcomes over time that adapts to the selected date range"
    - "Admin sees an upcoming callbacks table with lead name, broker name, scheduled time, and status"
  artifacts:
    - path: "src/components/calls/call-outcome-chart.tsx"
      provides: "Stacked bar chart of call outcomes over time using recharts"
      min_lines: 40
    - path: "src/components/calls/upcoming-callbacks.tsx"
      provides: "Table of upcoming pending callbacks with lead and broker details"
      min_lines: 30
    - path: "src/lib/queries/call-reporting.ts"
      provides: "fetchCallOutcomeVolume and fetchUpcomingCallbacks query functions"
      exports: ["fetchCallOutcomeVolume", "fetchUpcomingCallbacks"]
  key_links:
    - from: "src/app/(dashboard)/calls/page.tsx"
      to: "src/lib/queries/call-reporting.ts"
      via: "server-side fetch in page component"
      pattern: "fetchCallOutcomeVolume|fetchUpcomingCallbacks"
    - from: "src/components/calls/call-outcome-chart.tsx"
      to: "recharts"
      via: "BarChart with stacked bars for each outcome"
      pattern: "BarChart.*Bar.*dataKey"
    - from: "src/lib/queries/call-reporting.ts"
      to: "callbacks table"
      via: "supabase admin client query with lead/broker joins"
      pattern: "from\\('callbacks'\\)"
---

<objective>
Add call outcome chart and upcoming callbacks list to the /calls reporting page.

Purpose: Visualize call outcome trends over time (RPT-02) and surface upcoming scheduled callbacks (RPT-04).
Output: Stacked bar chart of outcomes by date bucket + callbacks table on the /calls page.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-call-reporting-dashboard/37-01-SUMMARY.md

Key patterns to follow:
- Chart: src/components/dashboard/lead-volume-chart.tsx (recharts BarChart, ResponsiveContainer, useTheme for dark mode)
- Volume query: fetchLeadVolume in src/lib/queries/dashboard.ts (date bucketing into daily/weekly)
- Table: shadcn Table/TableHeader/TableBody/TableRow/TableCell pattern from kpi-cards.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Call outcome volume and upcoming callbacks queries</name>
  <files>src/lib/queries/call-reporting.ts</files>
  <action>
Add two new functions to the existing src/lib/queries/call-reporting.ts:

**1. fetchCallOutcomeVolume(filters?: DashboardFilters):**
- Follow the fetchLeadVolume pattern from src/lib/queries/dashboard.ts for date bucketing
- Calculate totalDays from date range, use daily buckets if <= 30 days, weekly if > 30
- For each bucket, query call_logs with 4 separate count queries (one per outcome) filtered by bucket date range
- Apply optional broker_id filter to each query
- Return typed array: { date: string, label: string, transferred: number, callback_booked: number, no_answer: number, voicemail: number }[]
- Also return bucketType and totalDays like fetchLeadVolume does
- Use the same format() calls from date-fns for labels (EEE for <= 7 days, MMM d otherwise)

IMPORTANT: To avoid N+1 query explosion (4 queries per bucket * many buckets), use a more efficient approach:
- Query ALL call_logs in the date range in a single query: select('outcome, created_at') with optional broker_id filter
- Then bucket the results in TypeScript using a Map keyed by date string
- This is 1 query instead of potentially 120+ queries

**2. fetchUpcomingCallbacks(limit = 20):**
- Query callbacks table where status = 'pending' and scheduled_time >= now()
- Join leads (first_name, last_name) and brokers (first_name, last_name)
- Order by scheduled_time ascending (soonest first)
- Limit to `limit` rows
- Return typed array with: id, scheduled_time, status, lead (first_name, last_name), broker (first_name, last_name)
- No date filter needed here (always show upcoming regardless of date range selection)

Export return types: CallOutcomeVolume, UpcomingCallback[]
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && npx tsc --noEmit src/lib/queries/call-reporting.ts 2>&1 | head -20</automated>
  </verify>
  <done>fetchCallOutcomeVolume returns bucketed outcome data, fetchUpcomingCallbacks returns pending callbacks with lead/broker names. Both compile cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Call outcome chart, upcoming callbacks table, and page integration</name>
  <files>
    src/components/calls/call-outcome-chart.tsx
    src/components/calls/upcoming-callbacks.tsx
    src/app/(dashboard)/calls/page.tsx
  </files>
  <action>
**1. Create src/components/calls/call-outcome-chart.tsx:**
- 'use client' component following lead-volume-chart.tsx pattern
- Accept props: { data: CallOutcomeVolume['data'], bucketType, totalDays }
- Use recharts BarChart with stacked Bars (stackId="outcome"):
  - transferred: fill="#10b981" (emerald-500)
  - callback_booked: fill="#8b5cf6" (violet-500)
  - no_answer: fill="#f59e0b" (amber-500)
  - voicemail: fill="#f43f5e" (rose-500)
- Include Legend component from recharts below the chart
- XAxis with dataKey="label", YAxis with allowDecimals=false
- Tooltip with the same dark/light styling from lead-volume-chart.tsx (useTheme)
- Wrap in Card with CardHeader title "Call Outcomes" (same uppercase tracking style)
- Chart height: h-[250px]
- Generate dynamic title based on totalDays like getChartTitle pattern

**2. Create src/components/calls/upcoming-callbacks.tsx:**
- Server-compatible component (no 'use client' needed, it just renders data)
- Accept props: { callbacks: UpcomingCallback[] }
- Render inside a Card with CardHeader title "Upcoming Callbacks"
- Use shadcn Table with columns: Lead, Broker, Scheduled Time, Status
- Lead column: lead.first_name + lead.last_name
- Broker column: broker.first_name + broker.last_name
- Scheduled Time: format(new Date(scheduled_time), 'MMM d, h:mm a') using date-fns
- Status: capitalize, badge-style with text color (pending = amber)
- If no callbacks, show "No upcoming callbacks" centered message
- Use text-xs for table cells matching existing table patterns

**3. Update src/app/(dashboard)/calls/page.tsx:**
- Add fetchCallOutcomeVolume and fetchUpcomingCallbacks to the Promise.all
- Pass volume data to CallOutcomeChart below the KPI cards
- Add UpcomingCallbacks below the chart in a grid-cols-1 layout
- Layout order: Filters -> KPI Cards -> Chart (full width) -> Upcoming Callbacks (full width)
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Visit /calls, verify stacked bar chart renders with color-coded outcomes and upcoming callbacks table shows data</manual>
  </verify>
  <done>
    - Call outcome chart renders stacked bars with 4 outcome types in distinct colors
    - Chart adapts buckets when date range changes (daily vs weekly)
    - Upcoming callbacks table shows pending callbacks sorted by soonest
    - TypeScript compiles with no errors
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `bun run build` succeeds
- /calls page shows KPI cards, stacked bar chart, and upcoming callbacks table
- Changing date preset re-renders chart with appropriate buckets
- Selecting a broker scopes KPIs and chart (not callbacks list)
- Upcoming callbacks table shows lead name, broker name, scheduled time, status
</verification>

<success_criteria>
- Stacked bar chart shows call outcome distribution over time with color legend
- Chart adapts to daily/weekly buckets based on date range
- Upcoming callbacks table lists pending callbacks sorted by soonest
- Each callback row shows lead name, broker name, scheduled time, and pending status
- Full /calls page is complete with all 4 reporting requirements met
</success_criteria>

<output>
After completion, create `.planning/phases/37-call-reporting-dashboard/37-02-SUMMARY.md`
</output>
