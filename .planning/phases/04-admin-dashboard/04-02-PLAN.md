---
phase: 04-admin-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/(dashboard)/leads/page.tsx
  - src/app/(dashboard)/leads/[id]/page.tsx
  - src/components/leads/leads-data-table.tsx
  - src/components/leads/leads-columns.tsx
  - src/components/leads/leads-filters.tsx
  - src/components/leads/lead-detail.tsx
  - src/lib/queries/leads.ts
autonomous: true
requirements: [DASH-03, DASH-04, DASH-05, DASH-06]

must_haves:
  truths:
    - "Leads table shows all leads with columns: date/time, name, phone, email, vertical, credit score, funding amount, assigned broker, AI call status, assignment status"
    - "Leads table supports filtering by date range, vertical, credit score range, assigned/unassigned, broker"
    - "Leads table supports search by lead name, phone, or email"
    - "Clicking a lead row navigates to a detail view showing full data including AI call notes, assignment history, and webhook delivery status"
  artifacts:
    - path: "src/components/leads/leads-data-table.tsx"
      provides: "TanStack Table with server data, client-side column visibility"
      min_lines: 50
    - path: "src/components/leads/leads-filters.tsx"
      provides: "Filter bar with nuqs URL state for all filter dimensions"
      min_lines: 60
    - path: "src/components/leads/lead-detail.tsx"
      provides: "Full lead detail card with AI notes and delivery status"
      min_lines: 50
    - path: "src/lib/queries/leads.ts"
      provides: "Server-side lead queries with filtering/search/pagination"
      min_lines: 60
  key_links:
    - from: "src/app/(dashboard)/leads/page.tsx"
      to: "src/lib/queries/leads.ts"
      via: "server component calling fetchLeads with searchParams"
      pattern: "fetchLeads"
    - from: "src/components/leads/leads-filters.tsx"
      to: "nuqs"
      via: "useQueryState hooks for URL-synced filters"
      pattern: "useQueryState|parseAsString"
    - from: "src/app/(dashboard)/leads/[id]/page.tsx"
      to: "src/lib/queries/leads.ts"
      via: "server component calling fetchLeadDetail"
      pattern: "fetchLeadDetail"
---

<objective>
Build the leads table with TanStack Table, nuqs-powered filtering and search, and a lead detail view showing full data including AI call notes and webhook delivery status.

Purpose: The leads table is the primary operational view. Admin needs to find, filter, and inspect any lead in the system. Filters sync to URL so filtered views are shareable/bookmarkable.
Output: /leads page with full data table, working filters, search, and /leads/[id] detail page.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@src/lib/types/database.ts
@src/lib/supabase/admin.ts
@src/components/ui/table.tsx
@src/components/ui/input.tsx
@src/components/ui/select.tsx
@src/components/ui/badge.tsx
@.planning/phases/04-admin-dashboard/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lead query functions and leads data table with TanStack Table</name>
  <files>
    src/lib/queries/leads.ts
    src/components/leads/leads-columns.tsx
    src/components/leads/leads-data-table.tsx
    src/app/(dashboard)/leads/page.tsx
  </files>
  <action>
1. Create `src/lib/queries/leads.ts` with:
   - `fetchLeads(params)` function that accepts filter params object:
     - search?: string (matches first_name, last_name, phone, or email via ilike)
     - status?: 'assigned' | 'unassigned' | string
     - vertical?: string
     - broker_id?: string
     - credit_score_min?: number
     - credit_score_max?: number
     - date_from?: string (ISO date)
     - date_to?: string (ISO date)
     - page?: number (default 1)
     - per_page?: number (default 50)
   - Build query using createAdminClient: select leads with joined broker name (brokers!leads_assigned_broker_id_fkey(first_name, last_name))
   - Apply each filter conditionally: `.ilike('first_name', '%search%')` etc. For search, use `.or()` to match across name/phone/email
   - Apply date range with `.gte('created_at', date_from)` and `.lte('created_at', date_to)`
   - Apply pagination with `.range(offset, offset + per_page - 1)`
   - Order by created_at DESC
   - Return { data, count, error }
   - `fetchBrokersForFilter()`: simple query returning id, first_name, last_name for all brokers (used in filter dropdown)

2. Create `src/components/leads/leads-columns.tsx`:
   - Define TanStack Table column definitions using createColumnHelper or ColumnDef array
   - Columns: Date/Time (formatted with date-fns `format(date, 'MMM d, h:mm a')`), Name (first + last), Phone, Email, Vertical (Badge), Credit Score, Funding Amount (formatted as currency), Assigned Broker (name or 'Unassigned' badge), AI Call Status (badge), Status (colored badge: green=assigned, amber=unassigned)
   - Make the Name column a Link to /leads/[id]
   - Keep as a separate file so the table component stays clean

3. Create `src/components/leads/leads-data-table.tsx` as a client component ('use client'):
   - Import and use @tanstack/react-table: useReactTable, getCoreRowModel, flexRender
   - Accept data array and columns as props
   - Render using ShadCN Table components (Table, TableHeader, TableRow, TableHead, TableBody, TableCell)
   - Show row count in footer
   - Handle empty state: "No leads found matching your filters"

4. Create `src/app/(dashboard)/leads/page.tsx` as a server component:
   - Read searchParams from the page props (Next.js 16 server component searchParams)
   - Pass searchParams to fetchLeads() to get filtered data
   - Fetch brokers list for filter dropdown
   - Render page title "Leads" with count
   - Render LeadsFilters (client component, next task) passing brokers list
   - Render LeadsDataTable with data and columns
   - Add simple pagination controls (Previous/Next buttons) that update page searchParam
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && bun run build 2>&1 | tail -5</automated>
    <manual>Visit /leads to see the data table with lead records</manual>
  </verify>
  <done>Leads page renders a TanStack Table with all lead columns, server-side data from Supabase, pagination, and empty state handling. Column definitions include formatted dates, currency amounts, and colored status badges.</done>
</task>

<task type="auto">
  <name>Task 2: Add filters with nuqs URL state and lead detail view</name>
  <files>
    src/components/leads/leads-filters.tsx
    src/components/leads/lead-detail.tsx
    src/app/(dashboard)/leads/[id]/page.tsx
    src/lib/queries/leads.ts
  </files>
  <action>
1. Create `src/components/leads/leads-filters.tsx` as a client component ('use client'):
   - Use nuqs `useQueryState` with `parseAsString` for text filters, `parseAsInteger` for numeric filters
   - Filter controls in a horizontal bar:
     - Search input (text, debounced via nuqs `{ throttleMs: 300 }` option) for name/phone/email
     - Vertical select dropdown (options: MCA, SBA, Equipment Finance, Working Capital, Lines of Credit, All, plus "Any" default)
     - Status select (All, Assigned, Unassigned)
     - Broker select dropdown (populated from brokers prop)
     - Credit score range: min and max number inputs
     - Date range: from and to date inputs (type="date")
   - "Clear filters" button that resets all query params
   - Each filter change updates the URL via nuqs, which triggers a server component re-render with new searchParams
   - Wrap the page in NuqsAdapter from 'nuqs/adapters/next/app' in the leads page or layout

2. Add `fetchLeadDetail(id: string)` to `src/lib/queries/leads.ts`:
   - Fetch single lead by ID with all fields
   - Join assigned broker (first_name, last_name, company, email)
   - Join assigned order (id, verticals, total_leads, leads_delivered, status)
   - Fetch webhook_deliveries for this lead: select all delivery records ordered by created_at DESC
   - Fetch activity_log entries for this lead: select all activity records ordered by created_at DESC
   - Return { lead, broker, order, deliveries, activityLog }

3. Create `src/components/leads/lead-detail.tsx` as a server component:
   - Accept lead data, broker, order, deliveries, activityLog as props
   - Layout in ShadCN Cards:
     - Lead Info card: all lead fields in a 2-column grid (name, phone, email, business, funding amount, funding purpose, vertical, credit score, state, created_at)
     - AI Call card: ai_call_status badge + ai_call_notes in a pre-formatted block (or "No AI call data" empty state)
     - Assignment card: broker name (linked to /brokers/[id]), order ID, assigned_at timestamp, or "Unassigned" with reason if in unassigned queue
     - Delivery Status card: table of webhook_deliveries showing status badge, retry_count, sent_at, error_message. Color code: green=sent, amber=retrying, red=failed/failed_permanent, gray=pending
     - Activity History card: timeline of activity_log events for this lead

4. Create `src/app/(dashboard)/leads/[id]/page.tsx` as a server component:
   - Extract id from params
   - Call fetchLeadDetail(id)
   - Render a back link to /leads
   - Render page title with lead name
   - Render LeadDetail component with all fetched data
   - Handle not-found: if lead is null, show "Lead not found" message
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && bun run build 2>&1 | tail -5</automated>
    <manual>Visit /leads, use filters (search, vertical, status dropdowns), verify URL updates. Click a lead to see /leads/[id] detail page.</manual>
  </verify>
  <done>Leads table has working filters synced to URL via nuqs (search, vertical, status, broker, credit score range, date range). Lead detail page shows full data including AI call notes, assignment info, webhook delivery status, and activity history.</done>
</task>

</tasks>

<verification>
- `bun run build` passes with no errors
- /leads page renders data table with all specified columns
- Filters update URL params and table re-renders with filtered data
- Search works across name, phone, email
- /leads/[id] shows full lead detail with all cards
- Delivery status section shows webhook delivery records with color-coded badges
</verification>

<success_criteria>
- Leads table displays all required columns with proper formatting
- All 5 filter dimensions work (date, vertical, credit score, status, broker) plus search
- URL reflects current filter state (shareable filtered views)
- Lead detail view shows AI call notes, assignment info, delivery status, and activity history
- Empty states handled gracefully throughout
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-dashboard/04-02-SUMMARY.md`
</output>
