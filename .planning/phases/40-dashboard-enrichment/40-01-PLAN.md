---
phase: 40-dashboard-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/portal/queries.ts
  - src/components/portal/dashboard-enrichment.tsx
  - src/app/portal/(protected)/page.tsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05]

must_haves:
  truths:
    - "Broker sees a lead volume trend chart on their dashboard showing leads received over the selected date range"
    - "Broker sees a compact call summary card with total calls, transfer rate %, and next upcoming callback"
    - "Broker sees their average credit score across assigned leads within the selected date range"
    - "Broker sees a prominent next callback card with lead name and scheduled time when callbacks are pending"
    - "Changing the date range filter updates lead volume trend, call summary, and avg credit score"
  artifacts:
    - path: "src/lib/portal/queries.ts"
      provides: "fetchBrokerLeadVolumeTrend, fetchBrokerAvgCreditScore"
      contains: "fetchBrokerLeadVolumeTrend"
    - path: "src/components/portal/dashboard-enrichment.tsx"
      provides: "LeadVolumeTrendChart, CallSummaryCard, AvgCreditScoreCard, NextCallbackCard"
      contains: "LeadVolumeTrendChart"
    - path: "src/app/portal/(protected)/page.tsx"
      provides: "Updated dashboard with enrichment cards and reorganized layout"
      contains: "fetchBrokerLeadVolumeTrend"
  key_links:
    - from: "src/app/portal/(protected)/page.tsx"
      to: "src/lib/portal/queries.ts"
      via: "import fetchBrokerLeadVolumeTrend, fetchBrokerAvgCreditScore"
      pattern: "fetchBrokerLeadVolumeTrend"
    - from: "src/app/portal/(protected)/page.tsx"
      to: "src/lib/portal/call-queries.ts"
      via: "import fetchPortalCallKpis, fetchPortalUpcomingCallbacks"
      pattern: "fetchPortalCallKpis"
    - from: "src/app/portal/(protected)/page.tsx"
      to: "src/components/portal/dashboard-enrichment.tsx"
      via: "import LeadVolumeTrendChart, CallSummaryCard, AvgCreditScoreCard, NextCallbackCard"
      pattern: "LeadVolumeTrendChart"
    - from: "src/components/portal/dashboard-enrichment.tsx"
      to: "src/lib/portal/queries.ts"
      via: "type imports for LeadVolumeTrendData, AvgCreditScore"
      pattern: "LeadVolumeTrendData"
---

<objective>
Add lead volume trend, call summary, average credit score, and next callback cards to the broker portal dashboard, all controlled by the existing date range filter.

Purpose: Brokers currently see orders, spend, delivery health, spend trend, and recent leads. This enriches the dashboard with call activity awareness, lead quality signals, and callback urgency so brokers can act without navigating to sub-pages.

Output: Two new query functions, four new dashboard components, and a reorganized dashboard layout.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-call-reporting-page/39-01-SUMMARY.md

@src/lib/portal/queries.ts
@src/lib/portal/call-queries.ts
@src/lib/types/portal-filters.ts
@src/components/portal/dashboard-cards.tsx
@src/components/portal/spend-trend-chart.tsx
@src/components/portal/call-kpi-cards.tsx
@src/components/portal/upcoming-callbacks.tsx
@src/app/portal/(protected)/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lead volume trend and avg credit score queries</name>
  <files>src/lib/portal/queries.ts</files>
  <action>
Add two new exported query functions and their return types to the bottom of src/lib/portal/queries.ts (above the existing "Billing queries" section). Follow the exact pattern of existing functions in this file: createAdminClient(), broker_id filter, optional PortalDateFilters with getPortalDateRange.

**1. fetchBrokerLeadVolumeTrend(brokerId, dateFilters?)**

Returns `LeadVolumeTrendData` with shape:
```ts
export type LeadVolumeTrendData = {
  data: { date: string; label: string; count: number }[]
  bucketType: 'daily' | 'weekly'
  totalDays: number
}
```

Logic: Query leads table filtered by assigned_broker_id = brokerId and assigned_at within the date range. Use the same bucketing pattern from fetchPortalCallOutcomeVolume in call-queries.ts:
- Resolve date range via getPortalDateRange(dateFilters ?? {})
- Calculate totalDays with differenceInDays
- If totalDays > 30, use weekly buckets. Otherwise daily.
- Group leads by bucket in TypeScript (same Map-based approach)
- Generate zero-filled array for all buckets in the range
- Use format(day, 'EEE') for <= 7 days, format(day, 'MMM d') otherwise
- Import startOfDay, addDays, format, differenceInDays from date-fns

**2. fetchBrokerAvgCreditScore(brokerId, dateFilters?)**

Returns `AvgCreditScoreData` with shape:
```ts
export type AvgCreditScoreData = {
  average: number | null
  count: number
}
```

Logic: Query leads table filtered by assigned_broker_id = brokerId, credit_score is not null, and assigned_at within the date range. Select credit_score column. Calculate average in TypeScript (sum / count). Return null for average if count is 0.

Both functions must accept optional PortalDateFilters and use getPortalDateRange for date scoping. Add the date-fns imports (startOfDay, addDays, format, differenceInDays) to the existing import block at the top of the file.
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Check that both exported functions exist with correct types</manual>
  </verify>
  <done>fetchBrokerLeadVolumeTrend and fetchBrokerAvgCreditScore are exported from src/lib/portal/queries.ts, accept brokerId + optional dateFilters, and type-check cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Create dashboard enrichment components</name>
  <files>src/components/portal/dashboard-enrichment.tsx</files>
  <action>
Create a new file src/components/portal/dashboard-enrichment.tsx with four components. Follow the design patterns from dashboard-cards.tsx and call-kpi-cards.tsx (Card/CardHeader/CardTitle, lucide icons, text-sm font-medium titles, bg tint on accent cards, muted-foreground for secondary text).

**1. LeadVolumeTrendChart (client component)**
- 'use client' is NOT needed at file level. Instead, extract this one component into a separate section that IS a client component OR make the whole file 'use client'. Since the other 3 components are simple server components, make this one its own 'use client' component at the top of the file with the rest being server components below.
- Actually, the simplest approach: make the entire file 'use client' since NextCallbackCard needs no hooks but calling it from a server page with data props is fine.
- Props: `{ data: LeadVolumeTrendData['data'], bucketType: 'daily' | 'weekly', totalDays: number }`
- Use recharts AreaChart (not BarChart, to visually differentiate from spend trend). Import Area, AreaChart, XAxis, YAxis, Tooltip, ResponsiveContainer from recharts.
- Follow the exact Tooltip/axis styling from call-outcome-chart.tsx (isDark theme detection via useTheme).
- Use a blue-500 fill/stroke for the area (brand color for leads), with fillOpacity 0.15 for the area fill.
- Card icon: TrendingUp (lucide) with text-blue-400 color.
- Title: "Lead Volume" (simple, matches the concise style of other cards).
- Chart height: 200px (same as SpendTrendChart).
- Empty state: "No lead data yet." in text-sm text-muted-foreground.

**2. CallSummaryCard (compact call metrics for dashboard)**
- Props: `{ kpis: PortalCallKpis, nextCallback: PortalUpcomingCallback | null }`
- Import PortalCallKpis type from '@/lib/portal/call-queries' and PortalUpcomingCallback type from '@/lib/portal/call-queries'.
- Show three rows in a compact card:
  - Row 1: "Total Calls" with the count (right-aligned, font-semibold tabular-nums)
  - Row 2: "Transfer Rate" with percentage (kpis.transferred / kpis.totalCalls * 100, rounded). Show "0%" if totalCalls is 0.
  - Row 3: "Next Callback" with lead name + time if nextCallback exists, or "None scheduled" in muted text if null
- Card icon: Phone (lucide) with text-blue-400 color.
- Title: "Call Summary".
- Follow SpendSummaryCard's row layout pattern (flex items-center justify-between).

**3. AvgCreditScoreCard**
- Props: `{ data: AvgCreditScoreData }`
- Import AvgCreditScoreData type from '@/lib/portal/queries'.
- Display the average as a large number (text-3xl font-semibold tabular-nums) centered in the card.
- Below the number, show "{count} leads scored" in text-xs text-muted-foreground.
- If data.average is null, show "--" for the number and "No scored leads" for the subtitle.
- Card icon: BarChart3 (lucide) with text-amber-400 color.
- Title: "Avg. Credit Score".
- Add a subtle color indicator: if average >= 680 text-emerald-500, >= 600 text-amber-500, < 600 text-red-500. Apply to the number.

**4. NextCallbackCard (prominent, attention-grabbing)**
- Props: `{ callback: PortalUpcomingCallback | null }`
- Import PortalUpcomingCallback from '@/lib/portal/call-queries'.
- When callback is null: render a card with CalendarClock icon, "Next Callback" title, and body text "No upcoming callbacks" in muted foreground. Use bg-transparent (standard card look).
- When callback exists: render with a subtle violet tint (bg-violet-500/5 on the Card className, matching the KPI card pattern). Show:
  - Lead name (first_name + last_name, fallback "Unknown Lead") in text-base font-semibold
  - Scheduled time formatted as "MMM d, h:mm a" (using date-fns format) in text-sm text-muted-foreground with a Clock icon
- Card icon: CalendarClock (lucide) with text-violet-500 color.
- Title: "Next Callback".
- Import format from date-fns.
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Verify all 4 components are exported from the file</manual>
  </verify>
  <done>LeadVolumeTrendChart, CallSummaryCard, AvgCreditScoreCard, and NextCallbackCard are exported from src/components/portal/dashboard-enrichment.tsx and type-check cleanly</done>
</task>

<task type="auto">
  <name>Task 3: Wire enrichment into dashboard page with reorganized layout</name>
  <files>src/app/portal/(protected)/page.tsx</files>
  <action>
Update the portal dashboard page to fetch new data and render all enrichment components with a reorganized layout.

**New imports:**
- Add fetchBrokerLeadVolumeTrend, fetchBrokerAvgCreditScore from '@/lib/portal/queries' (extend existing import)
- Add fetchPortalCallKpis, fetchPortalUpcomingCallbacks from '@/lib/portal/call-queries' (new import)
- Add LeadVolumeTrendChart, CallSummaryCard, AvgCreditScoreCard, NextCallbackCard from '@/components/portal/dashboard-enrichment' (new import)

**Data fetching:**
Extend the existing Promise.all to include the 4 new fetches (total of 10 parallel fetches):
- fetchBrokerLeadVolumeTrend(brokerId, dateFilters)
- fetchBrokerAvgCreditScore(brokerId, dateFilters)
- fetchPortalCallKpis(brokerId, dateFilters)
- fetchPortalUpcomingCallbacks(brokerId) (no dateFilters, future-facing per Phase 39 decision)

Destructure as: leadVolume, avgCredit, callKpis, upcomingCallbacks alongside existing vars.

**Reorganized layout:**
The page is getting dense. Reorganize the JSX grid to fit new cards without clutter. Keep the existing header + PortalDateFilters at top.

New layout order:
```
1. Header + PortalDateFilters (unchanged)
2. Active Orders (full width, unchanged)
3. NEW: 3-column grid on md+ (1 col on mobile):
   - NextCallbackCard (first callback from upcomingCallbacks array, or null)
   - AvgCreditScoreCard
   - CallSummaryCard (pass callKpis + first upcoming callback)
4. Spend + Delivery Health side by side (unchanged, grid-cols-2)
5. NEW: Lead Volume Trend chart (full width)
6. Spend Trend chart (full width, unchanged)
7. Recent Leads (full width, unchanged)
```

For the NextCallbackCard, pass `upcomingCallbacks[0] ?? null` as the callback prop.
For the CallSummaryCard, pass `kpis={callKpis}` and `nextCallback={upcomingCallbacks[0] ?? null}`.

Use `grid grid-cols-1 md:grid-cols-3 gap-4` for the 3-card row (step 3). Use gap-4 (slightly tighter than the gap-6 used for major sections) to keep the compact cards visually grouped.
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Run `bun dev` and visit the portal dashboard to see enrichment cards render with data</manual>
  </verify>
  <done>Portal dashboard page fetches lead volume, avg credit score, call KPIs, and upcoming callbacks. All four enrichment components render in a reorganized layout. Changing the date filter updates lead volume trend, call summary, and avg credit score.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. Portal dashboard page renders without runtime errors
3. Lead volume trend chart shows area chart with daily/weekly bucketing
4. Call summary card shows total calls, transfer rate %, and next callback
5. Avg credit score card shows the average with color coding
6. Next callback card shows lead name + time (or empty state)
7. Changing date filter pill updates all new cards (server re-render via searchParams)
</verification>

<success_criteria>
- All 5 DASH requirements (DASH-01 through DASH-05) addressed
- 4 new components visible on dashboard
- Date range filter controls lead volume, call summary, and avg credit score
- Dashboard layout is clean and uncluttered despite new additions
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/40-dashboard-enrichment/40-01-SUMMARY.md`
</output>
