---
phase: 41-lead-quality-analytics
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - src/app/portal/(protected)/analytics/page.tsx
  - src/app/portal/(protected)/page.tsx
autonomous: true
requirements: [QUAL-03, QUAL-04, QUAL-05]

must_haves:
  truths:
    - "Broker can visit /portal/analytics and see full credit score histogram and vertical mix chart"
    - "Analytics page has its own PortalDateFilters bar controlling all charts"
    - "Changing date range on analytics page updates both charts"
    - "Dashboard page shows compact lead quality section with credit tier badges and vertical mix bar"
    - "Dashboard compact section responds to the dashboard date range filter"
  artifacts:
    - path: "src/app/portal/(protected)/analytics/page.tsx"
      provides: "Dedicated analytics page with full-size charts and date filter"
      contains: "CreditScoreHistogram"
    - path: "src/app/portal/(protected)/page.tsx"
      provides: "Dashboard with compact lead quality section"
      contains: "CompactCreditTiers"
  key_links:
    - from: "src/app/portal/(protected)/analytics/page.tsx"
      to: "src/lib/portal/queries.ts"
      via: "fetchBrokerCreditTierDistribution and fetchBrokerVerticalMix calls"
      pattern: "fetchBrokerCreditTierDistribution|fetchBrokerVerticalMix"
    - from: "src/app/portal/(protected)/analytics/page.tsx"
      to: "src/components/portal/lead-quality-charts.tsx"
      via: "CreditScoreHistogram and VerticalMixChart imports"
      pattern: "import.*CreditScoreHistogram|VerticalMixChart.*from.*lead-quality-charts"
    - from: "src/app/portal/(protected)/page.tsx"
      to: "src/components/portal/lead-quality-charts.tsx"
      via: "CompactCreditTiers and CompactVerticalMix imports"
      pattern: "import.*CompactCreditTiers|CompactVerticalMix.*from.*lead-quality-charts"
---

<objective>
Wire the lead quality components into the /portal/analytics page and the dashboard. Creates the new analytics route and updates the existing dashboard with a compact lead quality section.

Purpose: This makes the lead quality data visible to brokers in two contexts: a detailed analytics page for deep dives, and a quick glance summary on the dashboard.

Output: New /portal/analytics page with full charts + date filter, updated dashboard page with compact lead quality row.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-lead-quality-analytics/41-01-SUMMARY.md
@src/app/portal/(protected)/page.tsx
@src/app/portal/(protected)/calls/page.tsx
@src/lib/portal/queries.ts
@src/components/portal/lead-quality-charts.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /portal/analytics page with full-size charts and date filter</name>
  <files>src/app/portal/(protected)/analytics/page.tsx</files>
  <action>
Create `src/app/portal/(protected)/analytics/page.tsx`. Follow the exact same pattern as the /portal/calls/page.tsx:

```
export const dynamic = 'force-dynamic'
```

Server component that:
1. Calls `requireBrokerSession()` to get brokerId
2. Awaits searchParams, builds `PortalDateFiltersType` from params (same pattern as calls/page.tsx)
3. Calls `Promise.all` with:
   - `fetchBrokerCreditTierDistribution(brokerId, dateFilters)`
   - `fetchBrokerVerticalMix(brokerId, dateFilters)`
4. Renders inside `<NuqsAdapter>`:
   - Page header: h1 "Lead Quality Analytics", p description "Understand the credit profile and vertical distribution of your leads."
   - `<PortalDateFilters />` component (same shared filter bar from Phase 38)
   - Full-width `<CreditScoreHistogram data={creditTiers} />`
   - Full-width `<VerticalMixChart data={verticalMix} />`
5. Layout: `<div className="space-y-6 pt-8">` wrapping everything (same as calls page)

Imports:
- `NuqsAdapter` from 'nuqs/adapters/next/app'
- `requireBrokerSession` from '@/lib/portal/guard'
- `fetchBrokerCreditTierDistribution, fetchBrokerVerticalMix` from '@/lib/portal/queries'
- `CreditScoreHistogram, VerticalMixChart` from '@/components/portal/lead-quality-charts'
- `PortalDateFilters` from '@/components/portal/portal-date-filters'
- `PortalDateFilters as PortalDateFiltersType` from '@/lib/types/portal-filters'
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Check that /portal/analytics route file exists with CreditScoreHistogram and VerticalMixChart rendered</manual>
  </verify>
  <done>/portal/analytics page renders full credit score histogram and vertical mix chart with date filter bar, following the same server component pattern as /portal/calls</done>
</task>

<task type="auto">
  <name>Task 2: Add compact lead quality section to dashboard</name>
  <files>src/app/portal/(protected)/page.tsx</files>
  <action>
Update the existing dashboard page to include the compact lead quality components.

1. Add imports at the top:
   - `fetchBrokerCreditTierDistribution, fetchBrokerVerticalMix` from '@/lib/portal/queries'
   - `CompactCreditTiers, CompactVerticalMix` from '@/components/portal/lead-quality-charts'

2. Add the two new query calls to the existing `Promise.all` array:
   - `fetchBrokerCreditTierDistribution(brokerId, dateFilters)`
   - `fetchBrokerVerticalMix(brokerId, dateFilters)`
   Destructure them as `creditTiers` and `verticalMix` in the result array.

3. Add a new "Lead Quality" section in the layout. Place it AFTER the enrichment cards (3-column row with NextCallback/AvgCreditScore/CallSummary) and BEFORE the Spend + Delivery Health row. This groups the quality data near the existing credit score card.

   The section should be a 2-column grid:
   ```tsx
   {/* Lead Quality Summary */}
   <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
     <CompactCreditTiers data={creditTiers} />
     <CompactVerticalMix data={verticalMix} />
   </div>
   ```

This keeps the dashboard layout clean. The compact versions are small cards that fit naturally in the existing grid flow.
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Check that page.tsx has CompactCreditTiers and CompactVerticalMix in its render output</manual>
  </verify>
  <done>Dashboard shows compact lead quality row (credit tier badges + vertical mix bar) between the enrichment cards and the spend/delivery section, responding to date range filter</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- /portal/analytics route exists at src/app/portal/(protected)/analytics/page.tsx
- Analytics page imports and renders CreditScoreHistogram and VerticalMixChart
- Analytics page has PortalDateFilters component for date range control
- Dashboard page imports and renders CompactCreditTiers and CompactVerticalMix
- Dashboard fetches creditTiers and verticalMix data in its Promise.all
- Both pages pass dateFilters to the query functions
</verification>

<success_criteria>
- Broker can visit /portal/analytics and see full credit score histogram + vertical mix chart with date filtering (QUAL-04, QUAL-05)
- Dashboard shows compact lead quality summary that updates with date range changes (QUAL-03)
- All TypeScript compiles cleanly
- No admin-side imports, everything is portal-scoped
</success_criteria>

<output>
After completion, create `.planning/phases/41-lead-quality-analytics/41-02-SUMMARY.md`
</output>
