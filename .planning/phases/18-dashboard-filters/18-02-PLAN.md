---
phase: 18-dashboard-filters
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - src/app/(dashboard)/page.tsx
  - src/components/dashboard/kpi-cards.tsx
autonomous: true
requirements: [FILT-01, FILT-02, FILT-03, FILT-04, FILT-05, FILT-06]

must_haves:
  truths:
    - "Dashboard page accepts searchParams and passes parsed filters to all query functions"
    - "DashboardFilters component is rendered on the page with broker list"
    - "Selecting a date preset updates all KPI cards and delivery stats"
    - "Setting custom dates updates all dashboard data"
    - "Selecting a broker scopes all dashboard numbers to that broker"
    - "Selecting a vertical scopes all dashboard numbers to that vertical"
    - "URL reflects filter state and can be shared/bookmarked"
    - "Clear all resets to today with no broker/vertical filter"
  artifacts:
    - path: "src/app/(dashboard)/page.tsx"
      provides: "Dashboard page wired with filters, searchParams, NuqsAdapter"
      contains: "searchParams"
    - path: "src/components/dashboard/kpi-cards.tsx"
      provides: "KPI cards with updated subtitle for filtered mode"
  key_links:
    - from: "src/app/(dashboard)/page.tsx"
      to: "src/lib/queries/dashboard.ts"
      via: "passes parsed DashboardFilters to fetchKpis, fetchDeliveryStats, fetchLeadVolume, fetchRecentActivity"
      pattern: "fetchKpis\\(.*filters"
    - from: "src/app/(dashboard)/page.tsx"
      to: "src/components/dashboard/dashboard-filters.tsx"
      via: "renders DashboardFilters with brokers prop"
      pattern: "DashboardFilters.*brokers"
    - from: "src/app/(dashboard)/page.tsx"
      to: "nuqs"
      via: "NuqsAdapter wrapper"
      pattern: "NuqsAdapter"
---

<objective>
Wire the dashboard page to pass URL filter parameters to all query functions and render the filter component.

Purpose: This connects the filter UI (from Plan 01) to the data layer, making the full filtering flow work end-to-end.
Output: Fully functional filtered dashboard where all KPIs respond to date, broker, and vertical selections
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-dashboard-filters/18-01-SUMMARY.md
@src/app/(dashboard)/page.tsx
@src/app/(dashboard)/leads/page.tsx
@src/components/dashboard/kpi-cards.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire page.tsx with searchParams, filters, and NuqsAdapter</name>
  <files>src/app/(dashboard)/page.tsx</files>
  <action>
Update `src/app/(dashboard)/page.tsx` to:

1. Accept `searchParams` prop (same pattern as leads/page.tsx):
```
export default async function DashboardPage({
  searchParams,
}: {
  searchParams: Promise<Record<string, string | undefined>>
}) {
  const params = await searchParams
```

2. Parse filter values from params into a `DashboardFilters` object:
```
const filters: DashboardFilters = {
  date_preset: params.date_preset || undefined,
  date_from: params.date_from || undefined,
  date_to: params.date_to || undefined,
  broker_id: params.broker_id || undefined,
  vertical: params.vertical || undefined,
}
```

3. Import and fetch broker list for the dropdown using `fetchBrokersForFilter` from `@/lib/queries/leads` (reuse existing function, same one used by leads page).

4. Pass `filters` to all query functions:
```
const [kpis, activity, volume, deliveryStats, brokers] = await Promise.all([
  fetchKpis(filters),
  fetchRecentActivity(filters),
  fetchLeadVolume(filters),
  fetchDeliveryStats(filters),
  fetchBrokersForFilter(),
])
```

5. Import `NuqsAdapter` from `nuqs/adapters/next/app` and wrap the entire return JSX in `<NuqsAdapter>`.

6. Render `<DashboardFilters brokers={brokers} />` component between the page header and KpiCards. Import from `@/components/dashboard/dashboard-filters`.

7. Update imports:
- Add: `NuqsAdapter` from 'nuqs/adapters/next/app'
- Add: `DashboardFilters` component from '@/components/dashboard/dashboard-filters'
- Add: `fetchBrokersForFilter` from '@/lib/queries/leads'
- Add: `DashboardFilters` type from '@/lib/types/dashboard-filters'
- Change: `fetchLeadVolume7Days` to `fetchLeadVolume` (or use the alias if Plan 01 kept it)

8. Remove the old `fetchLeadVolume7Days` import if it was renamed in Plan 01.
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Open http://localhost:3000 and verify filter bar appears above KPI cards</manual>
  </verify>
  <done>Dashboard page reads searchParams, constructs DashboardFilters object, passes to all query functions, renders filter component with broker list, wrapped in NuqsAdapter.</done>
</task>

<task type="auto">
  <name>Task 2: Update KPI cards subtitle for filtered mode</name>
  <files>src/components/dashboard/kpi-cards.tsx</files>
  <action>
The "Leads Today" KPI card currently shows subtitle `${data.leadsThisWeek} this week / ${data.leadsThisMonth} this month`. Since Plan 01 removed the week/month queries (they return 0 now), update the subtitle:

1. In the KpiData interface, check if `leadsThisWeek` and `leadsThisMonth` are still in the return type. If Plan 01 set them to 0, update the "Leads Today" card config:
  - Change `title` from 'Leads Today' to 'Leads' (it's not always "today" anymore)
  - Change `subtitle` to show just the count context, e.g., `'In selected period'` or keep a simple count. If Plan 01 still returns week/month as 0, conditionally show the subtitle: if leadsThisWeek > 0, show the original format; otherwise show 'In selected period'.
  - Simplest approach: change subtitle to `'In selected period'`. The date filter bar above already tells the user what period they're looking at.

2. Similarly, update "Failed Deliveries" subtitle from the "today" framing to generic "in period" framing.

3. Update "Rejected Rate" subtitle: change `${data.rejectedCount} rejected out of ${data.leadsToday} today` to `${data.rejectedCount} rejected out of ${data.leadsToday} leads`.

4. The "Leads Today" card's `viewAllHref` currently hardcodes today's date: `/leads?date_from=${format(new Date(), 'yyyy-MM-dd')}`. This should still work but ideally would pass through the current filter. For now, leave as-is since View All is a convenience link. This is not a requirement.

5. Ensure the KpiData interface matches the updated return type from fetchKpis. If `leadsThisWeek` and `leadsThisMonth` were removed from the return type in Plan 01, remove them from the interface too. If they were kept as 0, keep the interface as-is and just update the display.
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Open http://localhost:3000, select "7 Days" filter, verify KPI card subtitles make sense and don't show "0 this week / 0 this month"</manual>
  </verify>
  <done>KPI cards display correct subtitles regardless of which date filter is active. No "0 this week / 0 this month" shown when non-today filter is selected. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `bun run build` succeeds (full Next.js build)
- Visit http://localhost:3000 with no params: shows today's data (default)
- Select "7 Days": URL updates to `?date_preset=7d`, KPI numbers change
- Select "30 Days": URL updates, numbers change
- Set custom date range: URL updates with `date_from` and `date_to`, preset clears
- Select a broker: URL adds `broker_id=xxx`, numbers scope to that broker
- Select a vertical: URL adds `vertical=MCA`, numbers scope to MCA leads
- Click Clear All: URL resets, shows today's unfiltered data
- Copy URL with filters, open in new tab: same filtered state appears
</verification>

<success_criteria>
- Dashboard page accepts searchParams and passes filters to all 4 query functions
- NuqsAdapter wraps page content, DashboardFilters component rendered with broker list
- Selecting any filter updates URL and triggers server refetch with updated data
- Clear all resets to default state (today, no broker, no vertical)
- URL is shareable and bookmarkable with filter state preserved
- TypeScript compiles, Next.js builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/18-dashboard-filters/18-02-SUMMARY.md`
</output>
