---
phase: 16-routing-audit-trail
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - src/lib/queries/leads.ts
  - src/components/leads/lead-detail.tsx
autonomous: true
requirements:
  - AUDIT-03

must_haves:
  truths:
    - "Admin can click a lead and see a Routing Audit section listing every order considered"
    - "Each order row shows eligible/disqualified status, individual component scores, and total score"
    - "The winning order is visually highlighted as selected"
    - "Disqualified orders show their disqualify_reason in human-readable format"
    - "Score breakdown shows credit_fit, capacity, tier_match, loan_fit, priority_bonus, urgency_bonus components"
  artifacts:
    - path: "src/lib/queries/leads.ts"
      provides: "fetchLeadDetail returns routing_logs with order and broker names"
      contains: "routing_logs"
    - path: "src/components/leads/lead-detail.tsx"
      provides: "RoutingAudit card with score breakdown table"
      contains: "Routing Audit"
  key_links:
    - from: "src/app/(dashboard)/leads/[id]/page.tsx"
      to: "src/components/leads/lead-detail.tsx"
      via: "routingLogs prop passed from fetchLeadDetail result"
      pattern: "routingLogs"
    - from: "src/lib/queries/leads.ts"
      to: "supabase routing_logs table"
      via: "select query with order/broker joins"
      pattern: "routing_logs"
    - from: "src/components/leads/lead-detail.tsx"
      to: "score_breakdown jsonb"
      via: "renders each component from the jsonb object"
      pattern: "score_breakdown"
---

<objective>
Build the routing audit UI on the lead detail page showing all orders scored with full breakdowns.

Purpose: Admins need to understand WHY a lead was assigned to a specific order. The routing audit section gives full transparency into the scoring decision, showing every order considered with their individual score components and disqualification reasons.

Output: Updated lead detail page with a Routing Audit card showing score breakdowns.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-routing-audit-trail/16-01-SUMMARY.md
@src/lib/queries/leads.ts
@src/components/leads/lead-detail.tsx
@src/app/(dashboard)/leads/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add routing logs query to fetchLeadDetail and pass to component</name>
  <files>src/lib/queries/leads.ts, src/app/(dashboard)/leads/[id]/page.tsx</files>
  <action>
**In leads.ts, update `fetchLeadDetail()`:**

Add a third parallel query to fetch routing_logs for the lead, joined with order and broker data:

```typescript
const [{ data: deliveries }, { data: activityLog }, { data: routingLogs }] = await Promise.all([
  // ... existing deliveries query ...
  // ... existing activityLog query ...
  supabase
    .from('routing_logs')
    .select(`
      id, order_id, broker_id, eligible, disqualify_reason,
      score_breakdown, total_score, fill_rate, selected, created_at,
      orders!inner ( id, verticals, total_leads, leads_delivered, credit_score_min, priority ),
      brokers!inner ( id, first_name, last_name, company )
    `)
    .eq('lead_id', id)
    .order('selected', { ascending: false })
    .order('total_score', { ascending: false }),
])
```

Return routingLogs in the result: `return { lead, deliveries: deliveries ?? [], activityLog: activityLog ?? [], routingLogs: routingLogs ?? [] }`

**In page.tsx:** Pass `routingLogs` to the LeadDetail component:

```tsx
<LeadDetail
  lead={result.lead}
  deliveries={result.deliveries}
  activityLog={result.activityLog}
  routingLogs={result.routingLogs}
/>
```
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && bunx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Verify fetchLeadDetail returns routingLogs array</manual>
  </verify>
  <done>fetchLeadDetail returns routingLogs with order/broker joins. Page passes routingLogs prop to LeadDetail component. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build Routing Audit card on lead detail page</name>
  <files>src/components/leads/lead-detail.tsx</files>
  <action>
**Update LeadDetail component props** to accept `routingLogs: any[]`.

**Add a new "Routing Audit" Card** between the Assignment card and Delivery Status card. Only render when `routingLogs.length > 0`.

Card structure:
- CardHeader with title "Routing Audit" and a subtitle showing count: "{N} orders evaluated"
- CardContent with a Table showing all scored orders

Table columns:
| Column | Content |
|--------|---------|
| Order | Short order ID (first 8 chars, monospace, linked to /orders/{id}) + broker name underneath in muted text |
| Status | Badge: green "Selected" if selected=true, blue "Eligible" if eligible=true, red "Disqualified" if eligible=false |
| Reason | If disqualified, show disqualify_reason in human-readable format (replace underscores with spaces, capitalize). Empty for eligible orders. |
| Credit Fit | score_breakdown.credit_fit formatted to 1 decimal (out of 40) |
| Capacity | score_breakdown.capacity formatted to 1 decimal (out of 30) |
| Tier Match | score_breakdown.tier_match as integer (out of 20) |
| Loan Fit | score_breakdown.loan_fit as integer (out of 10) |
| Bonuses | sum of score_breakdown.priority_bonus + score_breakdown.urgency_bonus (show +/- sign) |
| Total | score_breakdown.total formatted to 1 decimal, bold for selected row |

Visual details:
- Selected row: light green background `bg-green-50`
- Disqualified rows: light red background `bg-red-50/50`, score columns show "-" instead of 0
- Use the existing Badge, Table, TableHeader, TableBody, TableRow, TableCell, TableHead components from ShadCN
- Score columns should use `text-right` alignment and `tabular-nums font-mono text-xs` for number formatting
- Fill rate shown as percentage in a small muted text under the Capacity score: e.g., "22.5 (80% full)"

For disqualified orders, score columns show "-" since scores are all zero and meaningless.

Badge colors (matching existing codebase pattern):
- Selected: `bg-green-100 text-green-800`
- Eligible: `bg-blue-100 text-blue-800`
- Disqualified: `bg-red-100 text-red-800`
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && bunx tsc --noEmit 2>&1 | head -20 && grep -c "Routing Audit" src/components/leads/lead-detail.tsx</automated>
    <manual>Visit a lead detail page at /leads/{id} for an assigned lead and verify the Routing Audit card shows all orders with score breakdowns</manual>
  </verify>
  <done>Lead detail page shows Routing Audit card with all orders evaluated. Selected order highlighted green. Disqualified orders shown red with reason. All 6 score components displayed. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
1. `bunx tsc --noEmit` compiles clean
2. Lead detail page at `/leads/{id}` shows Routing Audit card for assigned leads
3. All orders (eligible + disqualified) appear in the audit table
4. Selected order is highlighted and marked
5. Score breakdown shows all 6 components (credit_fit, capacity, tier_match, loan_fit, priority_bonus, urgency_bonus)
6. Disqualified orders show their reason
</verification>

<success_criteria>
- Admin can click any assigned lead and see the full routing audit
- Every order considered is listed with status (selected/eligible/disqualified)
- Score components are individually visible: credit_fit (40), capacity (30), tier_match (20), loan_fit (10), bonuses
- Disqualified orders show their exclusion reason
- Selected order is visually prominent
- Page loads without errors for leads with no routing logs (empty state handled)
</success_criteria>

<output>
After completion, create `.planning/phases/16-routing-audit-trail/16-02-SUMMARY.md`
</output>
