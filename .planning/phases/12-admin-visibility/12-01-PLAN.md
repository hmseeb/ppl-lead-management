---
phase: 12-admin-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/queries/dashboard.ts
  - src/lib/actions/dashboard.ts
  - src/components/dashboard/kpi-cards.tsx
  - src/lib/queries/brokers.ts
  - src/components/brokers/broker-detail.tsx
autonomous: true
requirements: [VIS-01, VIS-02]

must_haves:
  truths:
    - "Admin sees a 'Queued' KPI card on the overview dashboard showing count of deliveries with status='queued'"
    - "Clicking the Queued KPI card expands a preview table of queued deliveries"
    - "Broker detail page displays contact_hours, custom_hours_start/end, weekend_pause, and timezone fields"
    - "Broker detail page shows a list of queued deliveries for that broker"
  artifacts:
    - path: "src/lib/queries/dashboard.ts"
      provides: "queuedCount in fetchKpis return"
      contains: "queuedCount"
    - path: "src/lib/actions/dashboard.ts"
      provides: "fetchQueuedPreview server action"
      exports: ["fetchQueuedPreview"]
    - path: "src/components/dashboard/kpi-cards.tsx"
      provides: "Queued KPI card with click-to-expand"
      contains: "Queued"
    - path: "src/lib/queries/brokers.ts"
      provides: "Queued deliveries in fetchBrokerDetail"
      contains: "queuedDeliveries"
    - path: "src/components/brokers/broker-detail.tsx"
      provides: "Hours info section and queued deliveries table"
      contains: "contact_hours"
  key_links:
    - from: "src/components/dashboard/kpi-cards.tsx"
      to: "src/lib/actions/dashboard.ts"
      via: "fetchQueuedPreview server action call"
      pattern: "fetchQueuedPreview"
    - from: "src/components/brokers/broker-detail.tsx"
      to: "src/lib/queries/brokers.ts"
      via: "queuedDeliveries prop from fetchBrokerDetail"
      pattern: "queuedDeliveries"
---

<objective>
Add queued delivery count KPI card to the overview dashboard and display broker hours info with queued deliveries on the broker detail page.

Purpose: Give admin at-a-glance visibility into queued deliveries across the system and per-broker hours configuration.
Output: Updated dashboard with Queued KPI card, updated broker detail page with hours and queued deliveries sections.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/queries/dashboard.ts
@src/lib/actions/dashboard.ts
@src/components/dashboard/kpi-cards.tsx
@src/lib/queries/brokers.ts
@src/components/brokers/broker-detail.tsx
@src/app/(dashboard)/brokers/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Queued KPI card to overview dashboard</name>
  <files>src/lib/queries/dashboard.ts, src/lib/actions/dashboard.ts, src/components/dashboard/kpi-cards.tsx</files>
  <action>
1. In `src/lib/queries/dashboard.ts` `fetchKpis()`:
   - Add a new parallel query: `supabase.from('deliveries').select('id', { count: 'exact', head: true }).eq('status', 'queued')`
   - Add `queuedCount: queued.count ?? 0` to the return object

2. In `src/lib/actions/dashboard.ts`:
   - Add `'queued'` to the `KpiPreviewType` union type
   - Add a new `fetchQueuedPreview` server action that queries:
     ```
     supabase.from('deliveries')
       .select('id, channel, status, created_at, broker_id, lead_id, brokers(first_name, last_name), leads(first_name, last_name)')
       .eq('status', 'queued')
       .order('created_at', { ascending: true })
       .limit(8)
     ```

3. In `src/components/dashboard/kpi-cards.tsx`:
   - Add `queuedCount: number` to the `KpiData` interface
   - Import `Clock` from lucide-react (represents waiting/queued)
   - Import `fetchQueuedPreview` from the actions file
   - Add a new card config object in the `cards` array (place it after "Unassigned"):
     - title: 'Queued'
     - value: data.queuedCount
     - subtitle: 'Waiting for broker hours'
     - icon: Clock
     - iconColor: 'text-orange-400'
     - glowColor: 'shadow-[0_0_20px_rgba(249,115,22,0.08)]'
     - highlight when queuedCount > 0
     - previewType: 'queued'
     - borderColor: 'border-orange-400'
     - viewAllHref: '/activity?event_type=delivery_queued' (links to activity page filtered)
     - fetchAction: fetchQueuedPreview
   - Update grid to `lg:grid-cols-6` (was 5, now 6 cards)
   - Add a `case 'queued'` to the `PreviewTable` switch statement showing columns: Lead, Broker, Channel, Queued At. Render broker name from the joined brokers relation, lead name from leads, channel as uppercase badge, and created_at formatted.
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Visit dashboard overview page, confirm 6 KPI cards render including "Queued"</manual>
  </verify>
  <done>Dashboard overview page shows 6 KPI cards in a row. "Queued" card displays count of deliveries with status='queued'. Clicking it expands a preview table with lead name, broker name, channel, and queued time.</done>
</task>

<task type="auto">
  <name>Task 2: Add hours info and queued deliveries to broker detail page</name>
  <files>src/lib/queries/brokers.ts, src/components/brokers/broker-detail.tsx, src/app/(dashboard)/brokers/[id]/page.tsx</files>
  <action>
1. In `src/lib/queries/brokers.ts` `fetchBrokerDetail()`:
   - The broker query already uses `select('*')` so contact_hours, custom_hours_start, custom_hours_end, weekend_pause, timezone are already returned
   - Add a third parallel query for queued deliveries:
     ```
     supabase.from('deliveries')
       .select('id, channel, created_at, lead_id, leads(first_name, last_name)')
       .eq('broker_id', id)
       .eq('status', 'queued')
       .order('created_at', { ascending: true })
     ```
   - Add `queuedDeliveries: queuedDeliveries ?? []` to the return object

2. In `src/components/brokers/broker-detail.tsx`:
   - Accept `queuedDeliveries` as a new prop alongside broker, orders, leads
   - Add a "Contact Hours" card (Card component) between Profile and Orders:
     - Title: "Contact Hours"
     - Grid layout showing:
       - "Schedule:" with value from broker.contact_hours (display 'Anytime', 'Business Hours (9-5)', or 'Custom' accordingly)
       - If custom, show "Window:" with broker.custom_hours_start - broker.custom_hours_end
       - "Weekend Pause:" with Yes/No based on broker.weekend_pause
       - "Timezone:" with broker.timezone or 'America/Los_Angeles (default)'
   - Add a "Queued Deliveries" card after Contact Hours (only if queuedDeliveries.length > 0):
     - Title: "Queued Deliveries ({count})"
     - Table with columns: Lead, Channel, Queued At
     - Show lead name from the joined leads relation, channel as badge, created_at formatted

3. In `src/app/(dashboard)/brokers/[id]/page.tsx`:
   - Pass `queuedDeliveries={result.queuedDeliveries}` to the BrokerDetail component
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Visit a broker detail page, confirm Contact Hours card and Queued Deliveries section appear</manual>
  </verify>
  <done>Broker detail page shows a "Contact Hours" card with schedule type, custom window (if applicable), weekend pause status, and timezone. If the broker has queued deliveries, a "Queued Deliveries" table appears showing lead name, channel, and queued time.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- Dashboard overview renders 6 KPI cards including "Queued"
- Broker detail page shows Contact Hours section with all hours fields
- Broker detail page shows queued deliveries when present
</verification>

<success_criteria>
- Queued delivery count visible on dashboard overview (VIS-01)
- Broker contact hours, timezone, weekend pause visible on broker detail page (VIS-02)
- Queued deliveries for a specific broker visible on their detail page (VIS-02)
</success_criteria>

<output>
After completion, create `.planning/phases/12-admin-visibility/12-01-SUMMARY.md`
</output>
