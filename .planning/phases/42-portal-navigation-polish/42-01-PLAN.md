---
phase: 42-portal-navigation-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/portal/portal-header.tsx
  - src/components/portal/call-kpi-cards.tsx
  - src/components/portal/call-outcome-chart.tsx
  - src/components/portal/upcoming-callbacks.tsx
  - src/components/portal/dashboard-enrichment.tsx
  - src/components/portal/lead-quality-charts.tsx
  - src/components/portal/dashboard-cards.tsx
  - src/components/portal/spend-trend-chart.tsx
  - src/components/portal/portal-date-filters.tsx
autonomous: true
requirements: [UX-03, UX-01]

must_haves:
  truths:
    - "Portal navigation shows Calls and Analytics links alongside existing links"
    - "Navigation highlights the active page correctly on /portal/calls and /portal/analytics"
    - "All v5.0 portal cards have consistent hover states (subtle lift/shadow transition)"
    - "All v5.0 chart components use uniform empty state messaging"
    - "The portal analytics experience looks cohesive and polished across all pages"
  artifacts:
    - path: "src/components/portal/portal-header.tsx"
      provides: "Updated nav with Calls + Analytics links"
      contains: "Calls"
    - path: "src/components/portal/call-kpi-cards.tsx"
      provides: "Polished KPI cards with hover states"
      contains: "transition"
    - path: "src/components/portal/dashboard-enrichment.tsx"
      provides: "Polished enrichment cards with hover states"
      contains: "transition"
    - path: "src/components/portal/lead-quality-charts.tsx"
      provides: "Polished chart cards with hover states"
      contains: "transition"
  key_links:
    - from: "src/components/portal/portal-header.tsx"
      to: "/portal/calls"
      via: "NAV_ITEMS href"
      pattern: "href.*portal/calls"
    - from: "src/components/portal/portal-header.tsx"
      to: "/portal/analytics"
      via: "NAV_ITEMS href"
      pattern: "href.*portal/analytics"
---

<objective>
Update portal navigation with Calls and Analytics links, then perform a design consistency polish pass across all v5.0 portal components.

Purpose: Complete v5.0 milestone by ensuring the portal feels like a cohesive, polished client-facing product with proper navigation to all new pages.
Output: Updated portal-header.tsx with 7 nav links, and all v5.0 components with consistent hover states, empty states, and visual refinements.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/portal/portal-header.tsx
@src/components/portal/call-kpi-cards.tsx
@src/components/portal/call-outcome-chart.tsx
@src/components/portal/upcoming-callbacks.tsx
@src/components/portal/dashboard-enrichment.tsx
@src/components/portal/lead-quality-charts.tsx
@src/components/portal/dashboard-cards.tsx
@src/components/portal/spend-trend-chart.tsx
@src/components/portal/portal-date-filters.tsx
@src/app/portal/(protected)/calls/page.tsx
@src/app/portal/(protected)/analytics/page.tsx
@src/app/portal/(protected)/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Calls and Analytics navigation links</name>
  <files>src/components/portal/portal-header.tsx</files>
  <action>
    Update the NAV_ITEMS array in portal-header.tsx to add two new entries between "Dashboard" and "Leads":

    1. Add `{ href: '/portal/calls', label: 'Calls', icon: Phone }` after Dashboard
    2. Add `{ href: '/portal/analytics', label: 'Analytics', icon: BarChart3 }` after Calls

    Import the icons from lucide-react: `Phone` and `BarChart3`.

    The final NAV_ITEMS order should be: Dashboard, Calls, Analytics, Leads, Orders, Billing, Settings.

    The existing `isActive` function already handles nested routes correctly via `pathname.startsWith(href)`, so /portal/calls and /portal/analytics will highlight properly without changes.

    No other changes to the header component. The existing styling is clean and consistent.
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Visit /portal/calls and /portal/analytics, confirm nav links appear and active state highlights correctly</manual>
  </verify>
  <done>Portal header shows 7 nav items (Dashboard, Calls, Analytics, Leads, Orders, Billing, Settings). Active page highlighting works on /portal/calls and /portal/analytics routes.</done>
</task>

<task type="auto">
  <name>Task 2: Design consistency polish pass across all v5.0 components</name>
  <files>
    src/components/portal/call-kpi-cards.tsx
    src/components/portal/call-outcome-chart.tsx
    src/components/portal/upcoming-callbacks.tsx
    src/components/portal/dashboard-enrichment.tsx
    src/components/portal/lead-quality-charts.tsx
    src/components/portal/dashboard-cards.tsx
    src/components/portal/spend-trend-chart.tsx
    src/components/portal/portal-date-filters.tsx
  </files>
  <action>
    Perform a systematic design polish pass to make all portal components feel cohesive and client-grade. Apply these specific changes:

    **1. Card hover states (all card-based components):**
    Add a subtle hover transition to every Card in portal components. Apply className `transition-shadow duration-200 hover:shadow-md hover:ring-foreground/15` alongside existing Card classes. This creates a gentle lift effect on hover that signals interactivity.

    Apply to Cards in:
    - call-kpi-cards.tsx (each KPI card)
    - call-outcome-chart.tsx (chart card)
    - upcoming-callbacks.tsx (callbacks card)
    - dashboard-enrichment.tsx (LeadVolumeTrendChart, CallSummaryCard, AvgCreditScoreCard, NextCallbackCard)
    - lead-quality-charts.tsx (CreditScoreHistogram, VerticalMixChart, CompactCreditTiers, CompactVerticalMix)
    - dashboard-cards.tsx (ActiveOrdersCard, RecentLeadsCard, SpendSummaryCard, DeliveryHealthCard)
    - spend-trend-chart.tsx (chart card)

    **2. Empty state consistency:**
    Standardize all empty state messages across chart/data components to use the same visual pattern. Each should use:
    - `<div className="flex flex-col items-center justify-center py-8 text-center">` wrapper
    - The component's header icon rendered at `size-8 text-muted-foreground/30 mb-2`
    - Message in `text-sm text-muted-foreground`

    Specific empty state text normalization (keep it short, no periods):
    - call-outcome-chart.tsx: "No call data in this period"
    - upcoming-callbacks.tsx: "No upcoming callbacks"  (already fine)
    - dashboard-enrichment.tsx LeadVolumeTrendChart: "No lead data in this period"
    - dashboard-enrichment.tsx AvgCreditScoreCard: keep as-is ("No scored leads" is fine, it's a metric not a chart)
    - dashboard-enrichment.tsx NextCallbackCard: "No upcoming callbacks" (already fine)
    - lead-quality-charts.tsx CreditScoreHistogram: "No scored leads in this period"
    - lead-quality-charts.tsx VerticalMixChart: "No lead data in this period"
    - lead-quality-charts.tsx CompactCreditTiers: "No scored leads" (already fine)
    - lead-quality-charts.tsx CompactVerticalMix: "No lead data" (already fine)
    - dashboard-cards.tsx empty states: leave as-is (these are pre-v5.0)
    - spend-trend-chart.tsx: "No spend data in this period"

    **3. Date filter bar refinement:**
    In portal-date-filters.tsx, add a subtle bottom border/separator after the filter bar to visually separate it from content below. Add `pb-2 border-b border-border/30` to the outer wrapper div. This creates visual rhythm between the filter controls and the data sections below.

    **4. Consistent chart card headers:**
    Ensure all chart Cards use the same CardHeader pattern. Currently all v5.0 chart cards use `flex flex-row items-center gap-2` which is good. Make sure none deviate.

    Do NOT change:
    - Color palette (it's already consistent: blue, emerald, violet, amber, rose)
    - Typography sizes (KPI cards intentionally use smaller uppercase labels, chart titles use text-sm)
    - Chart heights (intentionally varied: 200px dashboard, 250px calls, 300px analytics full-size)
    - Chart tooltip styles (already consistent across all recharts components)
    - Page-level layout structure (spacing, grid arrangements)
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Visit /portal, /portal/calls, /portal/analytics. Hover over cards to confirm subtle shadow lift. Check empty states render centered with icon.</manual>
  </verify>
  <done>All portal cards show consistent hover shadow transition. Empty states in chart components use centered layout with faded icon. Date filter bar has subtle bottom separator. No TypeScript errors.</done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes with zero errors
2. Portal navigation shows all 7 links in correct order
3. Active page highlighting works on /portal/calls and /portal/analytics
4. Card hover states visible across dashboard, calls, and analytics pages
5. Empty states render with centered icon + message pattern in chart components
</verification>

<success_criteria>
- Portal header has Dashboard, Calls, Analytics, Leads, Orders, Billing, Settings links
- /portal/calls nav link highlights when on that page
- /portal/analytics nav link highlights when on that page
- All portal cards have uniform hover shadow transition
- Chart empty states use consistent centered layout
- `npx tsc --noEmit` passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/42-portal-navigation-polish/42-01-SUMMARY.md`
</output>
