---
phase: 25-order-creation-payment
plan: 01
type: execute
wave: 1
depends_on: [24-pricing-engine]
files_modified:
  - supabase/migrations/00028_order_stripe_columns.sql
  - src/lib/types/database.ts
  - src/lib/stripe/client.ts
  - src/lib/schemas/portal-order.ts
  - src/lib/actions/portal-order.ts
  - src/app/api/stripe/webhook/route.ts
  - src/app/portal/(protected)/orders/new/page.tsx
  - src/components/portal/order-form.tsx
  - src/app/portal/(protected)/orders/success/page.tsx
  - src/app/portal/(protected)/orders/cancel/page.tsx
autonomous: true
requirements:
  - ORDER-01
  - ORDER-02
  - ORDER-03
  - ORDER-04
  - ORDER-05

must_haves:
  truths:
    - "Broker selects vertical, credit tier minimum, and lead count from portal order form"
    - "Order form shows calculated price per lead and total before checkout"
    - "Submitting redirects to Stripe Checkout with correct line items"
    - "checkout.session.completed webhook creates order in DB with status active"
    - "Abandoned/failed checkouts create no order record"
    - "Created order stores stripe_checkout_session_id and stripe_payment_intent_id"
  artifacts:
    - path: "supabase/migrations/00028_order_stripe_columns.sql"
      provides: "Add stripe_checkout_session_id, stripe_payment_intent_id, price_per_lead_cents, total_price_cents to orders"
    - path: "src/lib/stripe/client.ts"
      provides: "Stripe SDK singleton"
      exports: ["stripe"]
    - path: "src/lib/schemas/portal-order.ts"
      provides: "Zod schema for broker portal order form"
      exports: ["portalOrderSchema", "PortalOrderFormData"]
    - path: "src/lib/actions/portal-order.ts"
      provides: "Server action to create Stripe Checkout session"
      exports: ["createCheckoutSession"]
    - path: "src/app/api/stripe/webhook/route.ts"
      provides: "Stripe webhook handler for checkout.session.completed"
    - path: "src/app/portal/(protected)/orders/new/page.tsx"
      provides: "Order creation page rendering the form"
    - path: "src/components/portal/order-form.tsx"
      provides: "Client-side order form with live price calculation"
      exports: ["OrderForm"]
    - path: "src/app/portal/(protected)/orders/success/page.tsx"
      provides: "Post-payment success page"
    - path: "src/app/portal/(protected)/orders/cancel/page.tsx"
      provides: "Cancelled/abandoned checkout page"
  key_links:
    - from: "src/components/portal/order-form.tsx"
      to: "src/lib/actions/portal-order.ts"
      via: "form submission calls createCheckoutSession server action"
    - from: "src/lib/actions/portal-order.ts"
      to: "Stripe API"
      via: "stripe.checkout.sessions.create with price_data"
    - from: "src/app/api/stripe/webhook/route.ts"
      to: "supabase orders table"
      via: "inserts order on checkout.session.completed event"
    - from: "src/lib/actions/portal-order.ts"
      to: "src/lib/pricing/lookup.ts"
      via: "getLeadPrice for live price calculation"
---

<objective>
Build broker order creation with Stripe Checkout payment flow.

Purpose: Brokers create lead orders from the portal, pay via Stripe Checkout, and orders auto-activate on successful payment. No order record exists until payment succeeds.
Output: Migration, Stripe client, order form, checkout server action, webhook handler, success/cancel pages.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Install stripe + migration + types</name>
  <files>
    supabase/migrations/00028_order_stripe_columns.sql
    src/lib/types/database.ts
    src/lib/stripe/client.ts
  </files>
  <action>
    1. `bun add stripe`
    2. Create migration adding columns to orders:
       - stripe_checkout_session_id (text, nullable, unique)
       - stripe_payment_intent_id (text, nullable)
       - price_per_lead_cents (integer, nullable)
       - total_price_cents (integer, nullable)
    3. Update database.ts types for new order columns
    4. Create src/lib/stripe/client.ts with Stripe singleton
  </action>
</task>

<task type="auto">
  <name>Task 2: Schema + server action + webhook</name>
  <files>
    src/lib/schemas/portal-order.ts
    src/lib/actions/portal-order.ts
    src/app/api/stripe/webhook/route.ts
  </files>
  <action>
    1. Create Zod schema for portal order: vertical (single), credit_tier_min, lead_count
    2. Create server action: validate, lookup price, create Stripe Checkout session, return URL
    3. Create webhook handler: verify signature, handle checkout.session.completed, insert order
  </action>
</task>

<task type="auto">
  <name>Task 3: UI - order form + success/cancel pages</name>
  <files>
    src/app/portal/(protected)/orders/new/page.tsx
    src/components/portal/order-form.tsx
    src/app/portal/(protected)/orders/success/page.tsx
    src/app/portal/(protected)/orders/cancel/page.tsx
  </files>
  <action>
    1. Build order form with vertical select, credit tier select, lead count input, live price display
    2. Form submits to createCheckoutSession, redirects to Stripe
    3. Success page shows confirmation
    4. Cancel page shows message with link back to form
  </action>
</task>

</tasks>

<verification>
- TypeScript compiles without errors: `npx tsc --noEmit`
- Migration adds 4 nullable columns to orders table
- Order form shows correct price per lead from pricing engine
- Server action creates Stripe Checkout session with metadata
- Webhook creates order only on checkout.session.completed
- Success/cancel pages render correctly
</verification>

<success_criteria>
- Broker can create an order by selecting vertical, credit tier, and lead count
- Submitting redirects to Stripe Checkout with correct line items
- checkout.session.completed webhook creates order with status "active"
- Abandoned/failed checkouts create no order record
- Order record contains stripe_checkout_session_id and stripe_payment_intent_id
</success_criteria>
