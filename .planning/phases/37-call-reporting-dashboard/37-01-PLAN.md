---
phase: 37-call-reporting-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/calls/page.tsx
  - src/lib/queries/call-reporting.ts
  - src/components/calls/call-kpi-cards.tsx
  - src/components/calls/call-reporting-filters.tsx
  - src/components/layout/sidebar.tsx
autonomous: true
requirements: [RPT-01, RPT-03]

must_haves:
  truths:
    - "Admin sees a /calls page in the sidebar navigation"
    - "Admin sees KPI cards: total calls, transferred, callbacks booked, no answer, voicemail"
    - "Admin can filter by broker and date range, scoping all KPIs to that broker"
  artifacts:
    - path: "src/app/(dashboard)/calls/page.tsx"
      provides: "Call reporting page with server-side data fetching"
      min_lines: 30
    - path: "src/lib/queries/call-reporting.ts"
      provides: "Query functions for call KPIs with broker/date filtering"
      exports: ["fetchCallKpis"]
    - path: "src/components/calls/call-kpi-cards.tsx"
      provides: "KPI cards for call outcome counts"
      min_lines: 30
    - path: "src/components/calls/call-reporting-filters.tsx"
      provides: "Date preset and broker dropdown filters using nuqs"
      min_lines: 30
  key_links:
    - from: "src/app/(dashboard)/calls/page.tsx"
      to: "src/lib/queries/call-reporting.ts"
      via: "server-side fetch in page component"
      pattern: "fetchCallKpis"
    - from: "src/components/calls/call-reporting-filters.tsx"
      to: "page searchParams"
      via: "nuqs useQueryState with shallow:false for server refetch"
      pattern: "useQueryState.*serverSync"
    - from: "src/lib/queries/call-reporting.ts"
      to: "call_logs table"
      via: "supabase admin client queries"
      pattern: "from\\('call_logs'\\)"
---

<objective>
Create the call reporting page at /calls with KPI cards showing call outcome counts and broker/date filters.

Purpose: Give admin visibility into call activity (RPT-01) with broker-scoped filtering (RPT-03).
Output: Working /calls page with 5 KPI cards and filter controls.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-call-logging/35-01-SUMMARY.md

Key patterns to follow:
- Page pattern: src/app/(dashboard)/page.tsx (server component, NuqsAdapter, Promise<searchParams>)
- Filters pattern: src/components/dashboard/dashboard-filters.tsx (nuqs with shallow:false)
- KPI cards pattern: src/components/dashboard/kpi-cards.tsx (Card from shadcn, lucide icons)
- Query pattern: src/lib/queries/dashboard.ts (createAdminClient, getDateRange helper)
- Filter types: src/lib/types/dashboard-filters.ts (DashboardFilters, getDateRange, DATE_PRESETS)
- Sidebar: src/components/layout/sidebar.tsx (navItems array)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Call reporting queries and filter types</name>
  <files>src/lib/queries/call-reporting.ts</files>
  <action>
Create src/lib/queries/call-reporting.ts with:

1. `fetchCallKpis(filters?: DashboardFilters)` function that:
   - Uses createAdminClient() from @/lib/supabase/admin
   - Uses getDateRange(filters) from @/lib/types/dashboard-filters to derive from/to dates
   - Queries call_logs table with Promise.all for parallel counts:
     - Total calls: count all call_logs in date range
     - Transferred: count where outcome = 'transferred'
     - Callbacks booked: count where outcome = 'callback_booked'
     - No answer: count where outcome = 'no_answer'
     - Voicemail: count where outcome = 'voicemail'
   - Each query applies optional broker_id filter via .eq('broker_id', filters.broker_id)
   - Each query applies date range via .gte('created_at', from).lte('created_at', to)
   - Returns typed object: { totalCalls, transferred, callbacksBooked, noAnswer, voicemail }

Reuse the existing DashboardFilters type and getDateRange helper from @/lib/types/dashboard-filters.ts (no need to create new filter types, the existing date_preset + date_from + date_to + broker_id fields cover everything).

Follow the exact query pattern from fetchKpis in src/lib/queries/dashboard.ts:
- createAdminClient() for the client
- .select('id', { count: 'exact', head: true }) for count queries
- Return count ?? 0 for each

Export the return type as CallKpis for use in the KPI cards component.
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && npx tsc --noEmit src/lib/queries/call-reporting.ts 2>&1 | head -20</automated>
  </verify>
  <done>fetchCallKpis function compiles, accepts DashboardFilters, returns { totalCalls, transferred, callbacksBooked, noAnswer, voicemail } with broker and date filtering</done>
</task>

<task type="auto">
  <name>Task 2: Call reporting page, filters, KPI cards, and sidebar nav</name>
  <files>
    src/app/(dashboard)/calls/page.tsx
    src/components/calls/call-reporting-filters.tsx
    src/components/calls/call-kpi-cards.tsx
    src/components/layout/sidebar.tsx
  </files>
  <action>
**1. Create src/components/calls/call-reporting-filters.tsx:**
- 'use client' component
- Follow src/components/dashboard/dashboard-filters.tsx pattern exactly
- Include: DATE_PRESETS toggle buttons, date_from/date_to inputs, broker Select dropdown
- Use nuqs useQueryState with parseAsString and serverSync = { shallow: false }
- Accept brokers prop: { id: string; first_name: string; last_name: string }[]
- Include Clear button when any filter is active
- Do NOT include vertical filter or compare toggle (not relevant for call reporting)

**2. Create src/components/calls/call-kpi-cards.tsx:**
- 'use client' component following kpi-cards.tsx card layout pattern
- Accept props: { data: CallKpis } (import CallKpis type from queries)
- Render 5 cards in a responsive grid: grid-cols-2 md:grid-cols-3 lg:grid-cols-5
- Cards:
  - Total Calls: data.totalCalls, Phone icon, neutral color (blue-400)
  - Transferred: data.transferred, PhoneForwarded icon, emerald-400
  - Callbacks Booked: data.callbacksBooked, CalendarClock icon, violet-400
  - No Answer: data.noAnswer, PhoneMissed icon, amber-400
  - Voicemail: data.voicemail, Voicemail icon, rose-400
- Use lucide-react icons (Phone, PhoneForwarded, CalendarClock, PhoneMissed, Voicemail)
- Match existing KPI card styling: Card/CardHeader/CardTitle from shadcn, text-3xl font-bold font-mono for value, text-[11px] uppercase tracking-wider for title, text-[10px] text-muted-foreground for subtitle
- Each card shows a percentage subtitle: "X% of total" calculated from that outcome / totalCalls

**3. Create src/app/(dashboard)/calls/page.tsx:**
- export const dynamic = 'force-dynamic'
- Server component following src/app/(dashboard)/page.tsx pattern exactly
- Parse searchParams (Promise<Record<string, string | undefined>>)
- Build DashboardFilters from params (date_preset, date_from, date_to, broker_id)
- Fetch data with Promise.all: fetchCallKpis(filters), fetchBrokersForFilter()
- Import fetchBrokersForFilter from @/lib/queries/leads (already exists)
- Wrap in NuqsAdapter
- Render: h1 "Call Reporting" with gradient divider, CallReportingFilters, CallKpiCards
- Leave space below KPIs for Plan 02 to add chart and callbacks list

**4. Update src/components/layout/sidebar.tsx:**
- Add { href: '/calls', label: 'Calls', icon: Phone } to navItems array
- Import Phone from lucide-react
- Place it after Activity and before Settings in the nav order
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Visit /calls in the browser, verify KPI cards render and filters work</manual>
  </verify>
  <done>
    - /calls page renders with 5 KPI cards showing call outcome counts
    - Broker dropdown and date preset/range filters trigger server refetch via nuqs
    - "Calls" link appears in sidebar navigation between Activity and Settings
    - TypeScript compiles with no errors
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `bun run build` succeeds (no build errors)
- /calls page loads in browser with KPI cards
- Selecting a broker from dropdown re-renders KPIs scoped to that broker
- Date preset buttons (Today, 7d, 30d, 90d) re-render KPIs for that range
</verification>

<success_criteria>
- Admin can navigate to /calls from sidebar
- 5 KPI cards show: total calls, transferred, callbacks booked, no answer, voicemail
- Broker filter scopes all KPIs to selected broker
- Date filters scope KPIs to selected range
- Page follows existing dashboard patterns (server component, nuqs, admin client)
</success_criteria>

<output>
After completion, create `.planning/phases/37-call-reporting-dashboard/37-01-SUMMARY.md`
</output>
