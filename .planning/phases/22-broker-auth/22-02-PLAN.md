---
phase: 22-broker-auth
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - src/app/portal/login/page.tsx
  - src/app/portal/layout.tsx
  - src/app/portal/(protected)/layout.tsx
  - src/app/portal/(protected)/page.tsx
  - src/middleware.ts
  - src/components/brokers/broker-quick-actions.tsx
  - src/lib/actions/magic-link.ts
  - src/app/(dashboard)/brokers/[id]/page.tsx
autonomous: false
requirements:
  - AUTH-01
  - AUTH-03
  - AUTH-04

must_haves:
  truths:
    - "Broker sees a login form at /portal/login and can enter their email to request a magic link"
    - "Unauthenticated visit to /portal redirects to /portal/login"
    - "Authenticated broker at /portal sees a placeholder dashboard page with their name"
    - "Admin middleware does NOT interfere with /portal/* routes"
    - "Admin can click 'Invite to Portal' button on broker detail page to trigger magic link email"
  artifacts:
    - path: "src/app/portal/login/page.tsx"
      provides: "Broker login form with email input and magic link request"
      min_lines: 30
    - path: "src/app/portal/(protected)/layout.tsx"
      provides: "Auth guard layout that checks broker session and redirects unauthenticated to /portal/login"
      min_lines: 15
    - path: "src/app/portal/(protected)/page.tsx"
      provides: "Placeholder portal home showing broker name from session"
      min_lines: 10
    - path: "src/middleware.ts"
      provides: "Updated matcher excluding portal routes from admin auth"
      contains: "portal"
    - path: "src/components/brokers/broker-quick-actions.tsx"
      provides: "Updated with Invite to Portal button"
      contains: "Invite"
  key_links:
    - from: "src/app/portal/login/page.tsx"
      to: "src/lib/actions/magic-link.ts"
      via: "form action calling requestMagicLink"
      pattern: "sendMagicLink|requestMagicLink"
    - from: "src/app/portal/(protected)/layout.tsx"
      to: "src/lib/auth/broker-session.ts"
      via: "getBrokerSession check on every protected portal page load"
      pattern: "getBrokerSession"
    - from: "src/components/brokers/broker-quick-actions.tsx"
      to: "src/lib/actions/magic-link.ts"
      via: "invite action calling inviteBrokerToPortal"
      pattern: "inviteBrokerToPortal"
---

<objective>
Build the portal login UI, protect portal routes with broker auth, and add admin invite capability.

Purpose: Completes the broker auth flow end-to-end. Brokers can log in, portal routes are protected, and admins can trigger invite emails.
Output: Portal login page, portal layout with auth guard, placeholder portal home, updated middleware, invite button on broker detail
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-broker-auth/22-01-SUMMARY.md
@src/middleware.ts
@src/lib/auth/session.ts
@src/lib/auth/broker-session.ts
@src/lib/actions/magic-link.ts
@src/app/(auth)/login/page.tsx
@src/app/(auth)/layout.tsx
@src/components/brokers/broker-quick-actions.tsx
@src/app/(dashboard)/brokers/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Portal login page + layout auth guard + middleware update</name>
  <files>
    src/app/portal/login/page.tsx
    src/app/portal/layout.tsx
    src/app/portal/(protected)/layout.tsx
    src/app/portal/(protected)/page.tsx
    src/middleware.ts
    src/lib/actions/magic-link.ts
  </files>
  <action>
    1. Update `src/middleware.ts`:
       - Add `portal` to the matcher exclusion list so admin auth does NOT apply to /portal/* routes
       - Change matcher from `/((?!login|api|_next/static|_next/image|favicon.ico).*)` to `/((?!login|portal|api|_next/static|_next/image|favicon.ico).*)`
       - This is the ONLY change to middleware.ts. Broker auth is handled in the portal layout, not middleware.

    2. Create `src/app/portal/layout.tsx`:
       - Simple layout shell, no auth check
       - Wrapper div with portal-specific styling (clean, minimal, no admin sidebar)
       - ThemeProvider is inherited from root layout, so just a simple container div

    3. Create `src/app/portal/(protected)/layout.tsx`:
       - This is the auth guard. Uses Next.js route groups so login/verify routes bypass it.
       - Import getBrokerSession from '@/lib/auth/broker-session'
       - Import redirect from 'next/navigation'
       - Import createAdminClient from '@/lib/supabase/admin'
       - Get session, if !session.isBroker or !session.brokerId, redirect('/portal/login')
       - Fetch broker's first_name from brokers table using session.brokerId
       - Render a portal header with: "PPL Portal" brand left, broker name + logout button right
       - Create a brokerLogout server action that destroys broker session and redirects to /portal/login. Can be inline in this file or in a separate portal actions file.
       - Below header, render children in a padded container

    4. Create `src/app/portal/(protected)/page.tsx`:
       - Simple placeholder: "Welcome to your portal, {broker_name}. Your dashboard is coming soon."
       - Fetch broker info via getBrokerSession() + supabase query (it's a server component)
       - Show broker's email and name as a sanity check that auth is working

    5. Create `src/app/portal/login/page.tsx`:
       - 'use client' component
       - Similar visual style to admin login but with blue/portal branding instead of red admin branding
       - Email input field (type="email", required, autoFocus)
       - Submit button "Send Magic Link"
       - Use useActionState with a server action wrapper
       - On success: show a success message "Check your email for a login link" (do NOT reveal if email was found, always show this generic message for security)
       - On error from URL params (?error=invalid_link): show "This link has expired or already been used. Please request a new one."
       - Brand: "PPL Portal" with a subtle portal icon, tagline "Broker Access"

    6. Add `requestMagicLink` server action to `src/lib/actions/magic-link.ts`:
       - Signature: async function requestMagicLink(prevState: { error?: string; success?: boolean } | null, formData: FormData)
       - Extract email from formData
       - If no email, return { error: 'Email is required' }
       - Call sendMagicLink(email)
       - Always return { success: true } regardless of whether email was found (security: don't leak email existence). Only return error for truly unexpected failures.
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Visit /portal/login to see login form, visit /portal to verify redirect to /portal/login when not authenticated</manual>
  </verify>
  <done>Portal login page renders email form, unauthenticated /portal redirects to /portal/login, middleware excludes portal from admin auth check, authenticated portal shows placeholder with broker name</done>
</task>

<task type="auto">
  <name>Task 2: Admin invite button on broker detail page</name>
  <files>
    src/components/brokers/broker-quick-actions.tsx
    src/lib/actions/magic-link.ts
    src/app/(dashboard)/brokers/[id]/page.tsx
  </files>
  <action>
    1. Add `inviteBrokerToPortal` server action to `src/lib/actions/magic-link.ts`:
       - Accept brokerId: string
       - Use createAdminClient() to fetch broker's email from brokers table by id
       - If broker not found, return { error: 'Broker not found' }
       - Call sendMagicLink(broker.email) which handles token generation and email sending
       - Return { success: true } or { error: string }

    2. Update `src/components/brokers/broker-quick-actions.tsx`:
       - Add an "Invite to Portal" button with a Mail icon (from lucide-react)
       - Add inviting state (useState boolean)
       - On click: call inviteBrokerToPortal(brokerId) server action
       - On success: toast.success('Portal invite sent to broker')
       - On error: toast.error(result.error)
       - Place button between "Test Webhook" and "Edit" buttons in the toolbar
       - Accept broker email as prop (add `email: string` to BrokerQuickActionsProps) so the button can show tooltip with email being sent to
       - Show Loader2 spinner while inviting

    3. Update `src/app/(dashboard)/brokers/[id]/page.tsx`:
       - Pass email prop to BrokerQuickActions: email={result.broker.email}
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Go to broker detail page, see "Invite to Portal" button, click it to verify toast appears</manual>
  </verify>
  <done>Admin broker detail page has "Invite to Portal" button that calls inviteBrokerToPortal action, success/error toasts displayed, button integrates cleanly with existing quick actions toolbar</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete magic link auth flow</name>
  <action>
    Human verification checkpoint. All automated work is complete. Verify the end-to-end flow.
  </action>
  <verify>
    1. Visit /portal - should redirect to /portal/login
    2. Visit /portal/login - should see email input form with "PPL Portal" branding
    3. Enter a broker's email and submit - should see "Check your email" confirmation
    4. Apply the migration: `supabase db push` or run the migration SQL
    5. Set RESEND_API_KEY in Supabase edge function secrets: `supabase secrets set RESEND_API_KEY=re_xxxxx`
    6. Test full flow: enter broker email, check email inbox for magic link, click link, verify lands on /portal with broker name displayed
    7. Visit /brokers/{id} in admin, verify "Invite to Portal" button appears and sends email on click
    8. Visit /login (admin) - should still work normally, no interference
    9. Open browser dev tools, verify two separate cookies exist: ppl-session (admin) and ppl-broker-session (broker)
  </verify>
  <done>Full magic link auth flow verified: login form, email delivery, token verification, session creation, route protection, admin invite, no admin auth interference</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- /portal/login renders without errors
- /portal redirects to /portal/login when unauthenticated
- Existing admin login at /login still works
- Middleware matcher includes 'portal' in exclusion list
- Invite button visible on admin broker detail page
- Portal layout shows broker name when authenticated
</verification>

<success_criteria>
- Broker can enter email at /portal/login and receive magic link
- Clicking magic link creates broker session and lands on portal
- All /portal/* routes are protected (redirect to /portal/login if unauthenticated)
- Admin /login works unchanged
- Admin can invite broker from broker detail page
- Two separate session cookies (ppl-session, ppl-broker-session) coexist without conflict
</success_criteria>

<output>
After completion, create `.planning/phases/22-broker-auth/22-02-SUMMARY.md`
</output>
