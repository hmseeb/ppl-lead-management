---
phase: 34-callback-api-broker-availability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/leads/lookup/route.ts
autonomous: true
requirements: [AVAIL-01]

must_haves:
  truths:
    - "GET /api/leads/lookup?phone=X returns broker contact_hours, timezone, and weekend_pause when lead is assigned"
    - "Unassigned leads still return broker: null with no availability fields"
  artifacts:
    - path: "src/app/api/leads/lookup/route.ts"
      provides: "Leads lookup with broker availability fields"
      exports: ["GET"]
  key_links:
    - from: "src/app/api/leads/lookup/route.ts"
      to: "brokers table"
      via: "supabase select including contact_hours, timezone, weekend_pause"
      pattern: "contact_hours.*timezone.*weekend_pause"
---

<objective>
Extend the existing GET /api/leads/lookup endpoint to include broker availability fields (contact_hours, timezone, weekend_pause) in the response when a lead is assigned to a broker.

Purpose: Retell needs to know broker availability during calls to decide whether to transfer or book a callback.
Output: Updated leads lookup route with availability data.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/api/leads/lookup/route.ts
@src/lib/types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add broker availability to leads lookup response</name>
  <files>src/app/api/leads/lookup/route.ts</files>
  <action>
Modify the existing GET /api/leads/lookup endpoint in `src/app/api/leads/lookup/route.ts`:

1. Update the broker select query (line ~56) to include three additional columns: contact_hours, timezone, weekend_pause. The select string becomes: 'id, first_name, last_name, email, phone, company_name, ghl_contact_id, contact_hours, timezone, weekend_pause'

2. Add an `availability` object to the broker section of the response (inside the assigned=true branch, after the existing broker fields). The new response shape for broker becomes:
```
broker: {
  id: broker.id,
  name: [...],
  email: ghlDetails?.email ?? broker.email,
  phone: ghlDetails?.phone ?? broker.phone,
  company: ghlDetails?.companyName ?? broker.company_name,
  ghl_contact_id: broker.ghl_contact_id,
  availability: {
    contact_hours: broker.contact_hours,
    timezone: broker.timezone,
    weekend_pause: broker.weekend_pause,
  },
}
```

3. Do NOT change the unassigned lead response (broker: null case). No availability when no broker.

4. Do NOT change any other logic (phone parsing, lead lookup, GHL fetch). Minimal diff.
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && bunx tsc --noEmit 2>&1 | head -5</automated>
    <manual>GET /api/leads/lookup?phone=+1XXXXXXXXXX for an assigned lead returns broker.availability with contact_hours, timezone, weekend_pause fields</manual>
  </verify>
  <done>GET /api/leads/lookup returns broker.availability object with contact_hours, timezone, and weekend_pause for assigned leads. Unassigned leads return broker: null. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- `bunx tsc --noEmit` passes with zero errors
- GET /api/leads/lookup?phone=+1... for assigned lead includes broker.availability.contact_hours, broker.availability.timezone, broker.availability.weekend_pause
- GET /api/leads/lookup?phone=+1... for unassigned lead returns broker: null (unchanged)
- GET /api/leads/lookup without phone param still returns 400 (unchanged)
</verification>

<success_criteria>
- Broker availability fields (contact_hours, timezone, weekend_pause) included in lookup response
- No breaking changes to existing response shape (all existing fields preserved)
- Retell can determine broker availability from a single lookup call
</success_criteria>

<output>
After completion, create `.planning/phases/34-callback-api-broker-availability/34-02-SUMMARY.md`
</output>
