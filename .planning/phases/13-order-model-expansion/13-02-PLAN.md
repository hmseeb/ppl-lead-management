---
phase: 13-order-model-expansion
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/components/orders/order-form.tsx
  - src/components/orders/orders-table.tsx
  - src/components/orders/order-detail.tsx
  - src/lib/queries/orders.ts
autonomous: true
requirements:
  - ORDER-04

must_haves:
  truths:
    - "Order creation form has loan min and loan max input fields"
    - "Order creation form has priority select (normal/high)"
    - "Order creation form has order type select (one-time/monthly)"
    - "Orders table shows priority and order type columns"
    - "Order detail page displays loan range, priority, and order type"
  artifacts:
    - path: "src/components/orders/order-form.tsx"
      provides: "Form with loan_min, loan_max, priority, order_type fields"
      contains: "loan_min"
    - path: "src/components/orders/orders-table.tsx"
      provides: "Table displaying priority and order type columns"
      contains: "priority"
    - path: "src/components/orders/order-detail.tsx"
      provides: "Detail view showing all new order fields"
      contains: "loan_min"
    - path: "src/lib/queries/orders.ts"
      provides: "Queries selecting new fields"
      contains: "loan_min"
  key_links:
    - from: "src/components/orders/order-form.tsx"
      to: "src/lib/schemas/order.ts"
      via: "orderSchema import for validation"
      pattern: "import.*orderSchema"
    - from: "src/components/orders/order-form.tsx"
      to: "src/lib/actions/orders.ts"
      via: "createOrder server action call"
      pattern: "createOrder"
    - from: "src/components/orders/orders-table.tsx"
      to: "src/lib/queries/orders.ts"
      via: "data fetched with new fields"
      pattern: "priority.*order_type"
---

<objective>
Update the order creation form to include loan range, priority, and order type fields. Update the orders table and detail view to display these new fields.

Purpose: Admin needs to set and see all order properties when creating and reviewing orders. This completes the UI surface for the expanded order model.
Output: Updated form, table, and detail components with new field support.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-order-model-expansion/13-01-SUMMARY.md
@src/components/orders/order-form.tsx
@src/components/orders/orders-table.tsx
@src/components/orders/order-detail.tsx
@src/lib/queries/orders.ts
@src/lib/schemas/order.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new fields to order creation form</name>
  <files>src/components/orders/order-form.tsx</files>
  <action>
Update `src/components/orders/order-form.tsx` to add three new form sections after the existing Credit Score Min field:

1. **Loan Amount Range** (two side-by-side inputs):
   - Label: "Loan Amount Range (optional)"
   - Two number inputs in a `grid grid-cols-2 gap-3` layout:
     - `loan_min` with placeholder "Min ($)" - uses the same nullable number pattern as credit_score_min: `setValueAs: (v) => v === '' || v === undefined ? null : Number(v)`
     - `loan_max` with placeholder "Max ($)" - same pattern
   - Show validation error for loan_max (the refine error targets loan_max path)

2. **Priority** (native select, matching the existing broker_id select pattern):
   - Label: "Priority"
   - Options: "Normal" (value: "normal", default), "High" (value: "high")
   - Import PRIORITIES from schema and use `{...register('priority')}`

3. **Order Type** (native select):
   - Label: "Order Type"
   - Options: "One-time" (value: "one_time", default), "Monthly" (value: "monthly")
   - Import ORDER_TYPES from schema and use `{...register('order_type')}`

Update defaultValues to include: `loan_min: null, loan_max: null, priority: 'normal' as const, order_type: 'one_time' as const`

Use native HTML `<select>` elements (not ShadCN Select) per project convention (Phase 1 decision). Style them with the same className as the broker_id select: `"flex h-8 w-full rounded-lg border border-input bg-transparent px-2.5 py-1 text-sm transition-colors outline-none focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring/50"`

Display labels for order type as "One-time" and "Monthly" (user-friendly, not "one_time").
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && grep -q "loan_min" src/components/orders/order-form.tsx && grep -q "loan_max" src/components/orders/order-form.tsx && grep -q "priority" src/components/orders/order-form.tsx && grep -q "order_type" src/components/orders/order-form.tsx && bun run build 2>&1 | tail -5</automated>
    <manual>Visit /orders/new, confirm all fields are visible and the form submits correctly</manual>
  </verify>
  <done>Order form renders loan_min, loan_max, priority, and order_type fields with proper validation. Form submits successfully with new fields.</done>
</task>

<task type="auto">
  <name>Task 2: Display new fields in orders table and detail view</name>
  <files>src/components/orders/orders-table.tsx, src/components/orders/order-detail.tsx, src/lib/queries/orders.ts</files>
  <action>
**Update `src/lib/queries/orders.ts`:**

In `fetchOrdersWithBroker`, add `priority, order_type, loan_min, loan_max` to the select string (after `bonus_mode`).

In `fetchOrderDetail`, it already uses `*` so no change needed (the new columns come through automatically).

**Update `src/components/orders/orders-table.tsx`:**

1. Add `priority`, `order_type`, `loan_min`, `loan_max` to the `OrderWithBroker` type
2. Add two new table columns between "Credit Min" and "Status":
   - **Priority**: Show "High" with a Badge (variant="default", className for amber/orange styling like `bg-amber-100 text-amber-800 dark:bg-amber-900 dark:text-amber-200`) or just a dash for "normal" (normal is the default, only highlight high priority)
   - **Type**: Show "Monthly" as a Badge (variant="outline") or "One-time" as plain text
3. Add corresponding TableHead entries: "Priority" and "Type"

**Update `src/components/orders/order-detail.tsx`:**

Add three new info rows in the grid (before the Status row):
1. **Loan Range**: Display as "$X - $Y" when both set, "$X+" when only min, "Up to $Y" when only max, "Any" when both null. Format numbers with toLocaleString() for comma separators.
2. **Priority**: Display "High" with amber styling or "Normal" in plain text
3. **Order Type**: Display "Monthly" or "One-time"
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && grep -q "priority" src/components/orders/orders-table.tsx && grep -q "order_type" src/components/orders/orders-table.tsx && grep -q "loan_min\|Loan Range" src/components/orders/order-detail.tsx && bun run build 2>&1 | tail -5</automated>
    <manual>Visit /orders to see Priority and Type columns. Click an order to see loan range, priority, and type on detail page.</manual>
  </verify>
  <done>Orders table shows Priority and Type columns. Order detail page displays loan range, priority, and order type. Build passes with no errors.</done>
</task>

</tasks>

<verification>
1. Order form at /orders/new shows all new fields (loan min, loan max, priority, order type)
2. Creating an order with all fields populated persists correctly
3. Orders list table shows Priority and Type columns
4. Order detail page shows Loan Range, Priority, and Order Type
5. `bun run build` passes
</verification>

<success_criteria>
- Admin can create an order with loan_min=50000, loan_max=500000, priority=high, order_type=monthly
- New order appears in orders table with Priority "High" badge and Type "Monthly"
- Order detail page shows "Loan Range: $50,000 - $500,000", Priority: High, Type: Monthly
- All existing order functionality (status changes, bonus mode, etc.) still works
</success_criteria>

<output>
After completion, create `.planning/phases/13-order-model-expansion/13-02-SUMMARY.md`
</output>
