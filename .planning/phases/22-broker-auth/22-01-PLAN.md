---
phase: 22-broker-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00026_magic_links.sql
  - src/lib/types/database.ts
  - src/lib/auth/broker-session.ts
  - src/lib/actions/magic-link.ts
  - supabase/functions/send-magic-link/index.ts
  - src/app/portal/auth/verify/route.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02

must_haves:
  truths:
    - "Broker enters email and a magic link token is generated and stored in magic_links table"
    - "Magic link email is sent to the broker via Supabase Edge Function using Resend"
    - "Clicking the magic link validates the token and creates a broker session with broker_id"
    - "Expired or already-used tokens are rejected with a clear error"
    - "Broker session uses a separate cookie (ppl-broker-session) from admin session"
  artifacts:
    - path: "supabase/migrations/00026_magic_links.sql"
      provides: "magic_links table with token, broker_id, expires_at, used columns"
      contains: "CREATE TABLE magic_links"
    - path: "src/lib/auth/broker-session.ts"
      provides: "Broker session config and helpers (separate cookie from admin)"
      exports: ["getBrokerSession", "brokerSessionOptions", "BrokerSessionData"]
    - path: "src/lib/actions/magic-link.ts"
      provides: "Server actions for sending and verifying magic links"
      exports: ["sendMagicLink", "verifyMagicLink"]
    - path: "supabase/functions/send-magic-link/index.ts"
      provides: "Edge function that sends magic link email via Resend"
      contains: "Deno.serve"
    - path: "src/app/portal/auth/verify/route.ts"
      provides: "GET route handler that validates token and creates broker session"
      exports: ["GET"]
  key_links:
    - from: "src/lib/actions/magic-link.ts"
      to: "supabase magic_links table"
      via: "supabase admin client insert"
      pattern: "magic_links.*insert"
    - from: "src/lib/actions/magic-link.ts"
      to: "supabase/functions/send-magic-link"
      via: "supabase.functions.invoke"
      pattern: "functions\\.invoke.*send-magic-link"
    - from: "src/app/portal/auth/verify/route.ts"
      to: "src/lib/auth/broker-session.ts"
      via: "getBrokerSession to set broker_id"
      pattern: "getBrokerSession|brokerSession"

user_setup:
  - service: resend
    why: "Magic link email delivery"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard (resend.com) -> API Keys -> Create API Key"
    dashboard_config:
      - task: "Verify sending domain or use onboarding@resend.dev for testing"
        location: "Resend Dashboard -> Domains"
---

<objective>
Build the complete magic link authentication infrastructure for broker portal access.

Purpose: Brokers need passwordless login. This plan creates the token storage, email sending, verification, and session management. All backend plumbing, no UI yet.
Output: magic_links migration, broker session module, send/verify server actions, Supabase edge function for email, token verification route
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/middleware.ts
@src/lib/auth/session.ts
@src/lib/auth/actions.ts
@src/lib/supabase/admin.ts
@src/lib/supabase/server.ts
@src/lib/types/database.ts
@supabase/functions/send-alert/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Magic link table + broker session module</name>
  <files>
    supabase/migrations/00026_magic_links.sql
    src/lib/auth/broker-session.ts
    src/lib/types/database.ts
  </files>
  <action>
    1. Create migration `supabase/migrations/00026_magic_links.sql`:
       - CREATE TABLE magic_links with columns: id (uuid PK default gen_random_uuid()), token (text UNIQUE NOT NULL), broker_id (uuid NOT NULL REFERENCES brokers(id) ON DELETE CASCADE), expires_at (timestamptz NOT NULL), used (boolean NOT NULL DEFAULT false), created_at (timestamptz NOT NULL DEFAULT now())
       - CREATE INDEX idx_magic_links_token ON magic_links(token)
       - Token expiry: caller sets expires_at (15 minutes from creation)
       - Enable RLS on magic_links, add policy for service role only (no anon access)

    2. Create `src/lib/auth/broker-session.ts`:
       - Export interface BrokerSessionData { isBroker: boolean; brokerId: string }
       - Export brokerSessionOptions: SessionOptions with cookieName 'ppl-broker-session', password from process.env.SESSION_SECRET (same secret, different cookie name), httpOnly true, sameSite 'lax', secure in production
       - Export async function getBrokerSession() that uses cookies() + getIronSession<BrokerSessionData> with brokerSessionOptions
       - Follow exact same pattern as src/lib/auth/session.ts but with BrokerSessionData type

    3. Update `src/lib/types/database.ts`:
       - Add magic_links table types to the Database type under public.Tables
       - Row type: { id: string, token: string, broker_id: string, expires_at: string, used: boolean, created_at: string }
       - Insert type: { id?: string, token: string, broker_id: string, expires_at: string, used?: boolean, created_at?: string }
       - Update type: all optional except id
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Check migration SQL is valid, broker session uses separate cookie name</manual>
  </verify>
  <done>magic_links table migration exists, BrokerSessionData interface and getBrokerSession helper exported, database types updated with magic_links table</done>
</task>

<task type="auto">
  <name>Task 2: Magic link send/verify actions + edge function + verification route</name>
  <files>
    src/lib/actions/magic-link.ts
    supabase/functions/send-magic-link/index.ts
    src/app/portal/auth/verify/route.ts
  </files>
  <action>
    1. Create `src/lib/actions/magic-link.ts` as a 'use server' module:

       **sendMagicLink(email: string):**
       - Use createAdminClient() from src/lib/supabase/admin.ts
       - Look up broker by email (case-insensitive .ilike('email', email)) in brokers table. Return { error: 'no_broker' } if not found. Do NOT reveal whether email exists to the caller (the UI should show a generic "if this email exists" message regardless).
       - Generate token using crypto.randomUUID()
       - Insert into magic_links: { token, broker_id: broker.id, expires_at: new Date(Date.now() + 15 * 60 * 1000).toISOString() }
       - Build magic link URL: `${process.env.NEXT_PUBLIC_APP_URL}/portal/auth/verify?token=${token}`
       - Call supabase.functions.invoke('send-magic-link', { body: { to: broker.email, name: broker.first_name, link: magicLinkUrl } })
       - Return { success: true } on success, { error: string } on failure

       **verifyMagicLink(token: string):**
       - Use createAdminClient()
       - Query magic_links where token matches, used is false, expires_at > now()
       - If not found: return { error: 'invalid_or_expired' }
       - Mark token as used: update magic_links set used = true where id matches
       - Return { brokerId: row.broker_id }

    2. Create `supabase/functions/send-magic-link/index.ts`:
       - Follow the same Deno.serve pattern as send-alert/index.ts
       - Auth check: verify Bearer token in Authorization header
       - Parse body: { to: string, name: string, link: string }
       - Use Resend HTTP API (POST https://api.resend.com/emails) with RESEND_API_KEY from Deno.env
       - Send from: 'PPL Leads <noreply@pplleads.com>' (or RESEND_FROM_EMAIL env var)
       - Subject: 'Your Portal Login Link'
       - HTML body: clean, simple email with broker's first name greeting, the magic link as a button, and "This link expires in 15 minutes" note. Keep it minimal. Match the brand style (red accent color #dc2626).
       - Return { success: true, id: resendResponse.id } on 200, appropriate error responses on failure

    3. Create `src/app/portal/auth/verify/route.ts`:
       - Export async function GET(request: NextRequest)
       - Extract token from searchParams
       - If no token: redirect to /portal/login with ?error=missing_token
       - Call verifyMagicLink(token) from the actions module. Note: since this is a Route Handler (not a Server Component), calling a 'use server' function directly is fine, but to be safe, import the underlying logic directly or refactor verifyMagicLink to also be exported as a plain async function. Actually, in Next.js App Router route handlers, you CAN import and call server action functions directly, they just act as regular async functions in that context. So import verifyMagicLink from '@/lib/actions/magic-link'.
       - If error: redirect to /portal/login with ?error=invalid_link
       - On success: get broker session via getBrokerSession() using iron-session's getIronSession with request cookies. BUT note: in Route Handlers, you need to create a response first, then get session from request+response pair using getIronSession(request, response, brokerSessionOptions). Set session.isBroker = true, session.brokerId = result.brokerId, await session.save()
       - Redirect to /portal with the response that has the session cookie set
       - Use NextResponse.redirect() for all redirects
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Review magic link flow: sendMagicLink generates token + invokes edge function, verify route validates + creates session</manual>
  </verify>
  <done>sendMagicLink server action generates token and triggers email edge function, verifyMagicLink validates token and returns brokerId, /portal/auth/verify route creates broker session on valid token and redirects to /portal, edge function sends branded email via Resend API</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors: `npx tsc --noEmit`
- Migration file exists and has valid SQL syntax
- Broker session uses cookie name 'ppl-broker-session' (not 'ppl-session')
- sendMagicLink looks up broker by email, generates token, stores in DB, invokes edge function
- verifyMagicLink checks token validity (exists, not used, not expired)
- /portal/auth/verify route creates broker session with brokerId and redirects
- Edge function follows same Deno.serve pattern as existing functions
</verification>

<success_criteria>
- magic_links table migration ready to apply
- Broker session module with separate cookie from admin auth
- Complete send + verify flow for magic links
- Resend-based email edge function for sending magic link emails
- All TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/22-broker-auth/22-01-SUMMARY.md`
</output>
