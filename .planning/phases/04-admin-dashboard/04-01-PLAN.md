---
phase: 04-admin-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/components/layout/sidebar.tsx
  - src/components/dashboard/kpi-cards.tsx
  - src/components/dashboard/activity-feed.tsx
  - src/components/dashboard/lead-volume-chart.tsx
  - src/lib/queries/dashboard.ts
  - src/app/(dashboard)/page.tsx
autonomous: true
requirements: [DASH-01, DASH-02, UX-02]

must_haves:
  truths:
    - "Overview page shows KPI cards: leads today, this week, this month, assigned vs unassigned, active brokers, active orders"
    - "Overview page shows recent activity feed with last 20 events including timestamps, descriptions, and linked entities"
    - "Sidebar has nav links to all dashboard sections (Overview, Leads, Brokers, Orders, Unassigned, Activity)"
    - "Professional desktop UI using ShadCN Card components for KPIs"
  artifacts:
    - path: "src/components/dashboard/kpi-cards.tsx"
      provides: "KPI card grid with live counts from Supabase"
      min_lines: 40
    - path: "src/components/dashboard/activity-feed.tsx"
      provides: "Recent activity list with event descriptions"
      min_lines: 30
    - path: "src/lib/queries/dashboard.ts"
      provides: "Server-side data fetching for KPIs and activity"
      min_lines: 40
    - path: "src/app/(dashboard)/page.tsx"
      provides: "Overview page composing KPI cards and activity feed"
      min_lines: 20
  key_links:
    - from: "src/app/(dashboard)/page.tsx"
      to: "src/lib/queries/dashboard.ts"
      via: "server component calling query functions"
      pattern: "fetch(Kpis|Activity)"
    - from: "src/lib/queries/dashboard.ts"
      to: "supabase"
      via: "admin client queries"
      pattern: "createAdminClient"
---

<objective>
Install Phase 4 dependencies (nuqs, recharts, date-fns), build the overview dashboard with KPI cards and activity feed, and update sidebar navigation for all new sections.

Purpose: The overview page is the landing page for the admin. It must show system health at a glance: lead volume, assignment status, active brokers/orders, and recent activity. This plan also installs all shared deps that subsequent plans need.
Output: Working overview dashboard at / with KPI cards, activity feed, and updated sidebar. nuqs, recharts, date-fns installed.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@src/app/(dashboard)/page.tsx
@src/app/(dashboard)/layout.tsx
@src/components/layout/sidebar.tsx
@src/lib/supabase/admin.ts
@src/lib/types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install deps, create dashboard queries, and build KPI cards</name>
  <files>
    package.json
    src/lib/queries/dashboard.ts
    src/components/dashboard/kpi-cards.tsx
    src/components/dashboard/lead-volume-chart.tsx
  </files>
  <action>
1. Install new dependencies:
   `bun add nuqs recharts date-fns`

2. Create `src/lib/queries/dashboard.ts` with server-side query functions using createAdminClient():
   - `fetchKpis()`: Returns object with:
     - leadsToday: count of leads where created_at >= start of today (UTC)
     - leadsThisWeek: count where created_at >= start of current week (Monday)
     - leadsThisMonth: count where created_at >= start of current month
     - assignedCount: count where status = 'assigned'
     - unassignedCount: count where status = 'unassigned'
     - activeBrokers: count of brokers where assignment_status = 'active'
     - activeOrders: count of orders where status = 'active'
   - Use Supabase `.select('id', { count: 'exact', head: true })` with filters for efficient counting (no data transfer, just counts)
   - `fetchRecentActivity(limit = 20)`: Query activity_log ordered by created_at DESC, limit 20, with joins to brokers (first_name, last_name), leads (first_name, last_name), and orders (id). Return typed array.

3. Create `src/components/dashboard/kpi-cards.tsx` as a server component:
   - Import ShadCN Card, CardContent, CardHeader, CardTitle
   - Import lucide icons: Users, ShoppingCart, TrendingUp, AlertCircle, Activity, Package
   - Grid layout: 4 columns on lg, 2 on md, 1 on sm
   - Cards: "Leads Today" (with week/month as secondary text), "Assigned", "Unassigned", "Active Brokers", "Active Orders"
   - Each card shows the count as a large number with an icon
   - Use appropriate colors: unassigned count in amber/warning if > 0

4. Create `src/components/dashboard/lead-volume-chart.tsx` as a client component ('use client'):
   - Use recharts BarChart to show lead volume for the last 7 days
   - Accept data prop: Array of { date: string, count: number }
   - Add a `fetchLeadVolume7Days()` function to dashboard.ts that groups leads by date for the last 7 days
   - Use ShadCN Card wrapper, recharts ResponsiveContainer, BarChart, Bar, XAxis, YAxis, Tooltip
   - Format dates using date-fns `format(date, 'EEE')` for day names
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && bun run build 2>&1 | tail -5</automated>
    <manual>Check that nuqs, recharts, date-fns appear in package.json dependencies</manual>
  </verify>
  <done>Dashboard query functions exist and type-check. KPI cards component renders a grid of ShadCN cards. Lead volume chart component renders a recharts BarChart. All three new deps installed.</done>
</task>

<task type="auto">
  <name>Task 2: Build activity feed, update sidebar, and compose overview page</name>
  <files>
    src/components/dashboard/activity-feed.tsx
    src/components/layout/sidebar.tsx
    src/app/(dashboard)/page.tsx
  </files>
  <action>
1. Create `src/components/dashboard/activity-feed.tsx` as a server component:
   - Receives activity data array as prop (fetched by parent page)
   - Renders a ShadCN Card with title "Recent Activity"
   - Each activity item shows: relative time (using date-fns `formatDistanceToNow`), event description, and linked entity names
   - Format event_type to human-readable text: "lead_assigned" -> "Lead assigned to [broker name]", "order_created" -> "New order created", "order_status_changed" -> "Order [paused/resumed/completed]", "bonus_mode_toggled" -> "Bonus mode [enabled/disabled]", "webhook_failed_permanent" -> "Webhook delivery failed", "manual_assignment" -> "Lead manually assigned to [broker]", etc.
   - Use lucide icons per event type for visual distinction
   - Show "No recent activity" empty state
   - Truncate long lists, show scrollable container with max-h

2. Update `src/components/layout/sidebar.tsx`:
   - Add nav items for new sections: Leads (/leads, FileText icon), Unassigned (/unassigned, AlertCircle icon), Activity (/activity, Activity icon)
   - Keep existing: Overview (/), Brokers (/brokers), Orders (/orders)
   - Order: Overview, Leads, Brokers, Orders, Unassigned, Activity
   - Keep the logout button at bottom

3. Rewrite `src/app/(dashboard)/page.tsx` as a server component:
   - Import and call fetchKpis(), fetchRecentActivity(), fetchLeadVolume7Days() from dashboard queries
   - Render page title "Overview"
   - Render KpiCards component with kpi data
   - Render LeadVolumeChart with 7-day data (in a Card wrapper)
   - Render ActivityFeed with recent activity data
   - Layout: KPI cards on top, then a 2-column grid (chart left, activity feed right) on lg, stacked on smaller screens
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && bun run build 2>&1 | tail -5</automated>
    <manual>Run `bun dev` and visit http://localhost:3000 to see the overview dashboard with KPI cards, chart, and activity feed</manual>
  </verify>
  <done>Overview page at / shows KPI cards with real data from Supabase, a 7-day lead volume bar chart, and a recent activity feed. Sidebar has links to all 6 sections (Overview, Leads, Brokers, Orders, Unassigned, Activity).</done>
</task>

</tasks>

<verification>
- `bun run build` passes with no errors
- Overview page renders KPI cards with numeric counts
- Activity feed shows formatted event descriptions with relative timestamps
- Sidebar shows all 6 navigation items with correct icons and active states
- Lead volume chart renders (may show empty data if no leads exist)
</verification>

<success_criteria>
- KPI cards display: leads today/week/month, assigned count, unassigned count, active brokers, active orders
- Activity feed shows last 20 events with human-readable descriptions
- Sidebar updated with Leads, Unassigned, Activity nav items
- nuqs, recharts, date-fns installed in package.json
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-dashboard/04-01-SUMMARY.md`
</output>
