---
phase: 33-export-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/actions/portal-export.ts
  - src/components/portal/leads-table.tsx
autonomous: true
requirements: [EXPT-01]

must_haves:
  truths:
    - "Broker can click an export button on the leads page and download a CSV file"
    - "CSV contains the currently filtered leads with all visible columns"
    - "Export respects active search/vertical/delivery status filters"
  artifacts:
    - path: "src/lib/actions/portal-export.ts"
      provides: "Server action that generates CSV text from filtered leads"
      exports: ["exportLeadsCsv"]
    - path: "src/components/portal/leads-table.tsx"
      provides: "Export button in leads table header"
      contains: "exportLeadsCsv"
  key_links:
    - from: "src/components/portal/leads-table.tsx"
      to: "src/lib/actions/portal-export.ts"
      via: "server action call on button click"
      pattern: "exportLeadsCsv"
    - from: "src/lib/actions/portal-export.ts"
      to: "src/lib/portal/queries.ts"
      via: "uses fetchBrokerLeadsPaginated with no pagination limit"
      pattern: "fetchBrokerLeadsPaginated"
---

<objective>
CSV export for broker leads page. Broker clicks an export button and downloads a CSV file containing their filtered leads with all visible columns (name, vertical, credit score, funding amount, delivery status, assigned date).

Purpose: Brokers need to get their lead data out of the portal for use in spreadsheets, CRM imports, or reporting.
Output: Server action + export button integrated into existing leads table.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@src/lib/portal/queries.ts
@src/components/portal/leads-table.tsx
@src/lib/actions/portal-deliveries.ts
@src/lib/portal/guard.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CSV export server action</name>
  <files>src/lib/actions/portal-export.ts</files>
  <action>
Create a new server action file following the exact pattern of portal-deliveries.ts (use 'use server' directive, requireBrokerSession guard).

Export an async function `exportLeadsCsv` that accepts optional filter params `{ search?: string; vertical?: string; deliveryStatus?: string }`.

Implementation:
1. Call requireBrokerSession() to get brokerId
2. Call fetchBrokerLeadsPaginated(brokerId, 1, 10000, filters) to get ALL matching leads (use large perPage to get everything, not paginated)
3. Build CSV string with headers: Name,Vertical,Credit Score,Funding Amount,Delivery Status,Assigned Date
4. For each lead, format a CSV row:
   - Name: `${first_name} ${last_name}` (handle nulls with empty string)
   - Vertical: lead.vertical or empty
   - Credit Score: lead.credit_score or empty
   - Funding Amount: lead.funding_amount or empty (raw number, no formatting)
   - Delivery Status: lead.delivery_status or empty
   - Assigned Date: format lead.assigned_at as ISO date string or empty
5. Escape any field containing commas or quotes by wrapping in double quotes (standard CSV escaping: double any internal quotes)
6. Return the CSV string (the client will handle blob download)

Do NOT use any CSV library. Hand-roll it. It is 6 columns, not worth a dependency.
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && npx tsc --noEmit src/lib/actions/portal-export.ts 2>&1 | head -20</automated>
    <manual>Verify the file exports exportLeadsCsv, uses requireBrokerSession, and generates CSV text</manual>
  </verify>
  <done>Server action exportLeadsCsv exists, type-checks, calls requireBrokerSession for auth, returns CSV string with proper headers and escaping</done>
</task>

<task type="auto">
  <name>Task 2: Add export button to leads table with blob download</name>
  <files>src/components/portal/leads-table.tsx</files>
  <action>
Modify the existing LeadsTable component (already a client component with 'use client').

1. Import `exportLeadsCsv` from `@/lib/actions/portal-export`
2. Import `Download` icon from lucide-react (add to existing import)
3. Add a state variable `exporting` (boolean, default false)
4. Create an async function `handleExport`:
   - Set exporting = true
   - Build filter object from filterParams prop: { search: filterParams?.search, vertical: filterParams?.vertical, deliveryStatus: filterParams?.delivery_status } (only include non-empty values)
   - Call const csv = await exportLeadsCsv(filters)
   - Create a Blob: new Blob([csv], { type: 'text/csv;charset=utf-8;' })
   - Create object URL: URL.createObjectURL(blob)
   - Create temp anchor element, set href to URL, download attribute to `leads-export-${new Date().toISOString().slice(0,10)}.csv`
   - Click it, then revoke the object URL
   - Set exporting = false (in finally block)
5. Add the export button in the header area (the div with "All Leads" and badge), to the right of the badge:
   ```
   <Button variant="outline" size="sm" className="h-7 text-xs ml-auto" onClick={handleExport} disabled={exporting || leads.length === 0}>
     <Download className="size-3 mr-1" />
     {exporting ? 'Exporting...' : 'Export CSV'}
   </Button>
   ```

Keep all existing functionality intact. Only add the export button and handler.
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && npx tsc --noEmit src/components/portal/leads-table.tsx 2>&1 | head -20</automated>
    <manual>Verify Export CSV button appears in the leads table header area</manual>
  </verify>
  <done>Export CSV button visible in leads table header, clicking it calls server action and triggers browser file download, button disabled during export and when no leads</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors: `npx tsc --noEmit`
- Export button renders in the leads table header alongside the "All Leads" title
- Server action is session-protected (requireBrokerSession)
- CSV includes all visible columns with proper headers
- File download triggers with date-stamped filename
</verification>

<success_criteria>
- Broker sees "Export CSV" button on leads page
- Clicking it downloads a .csv file with all filtered leads
- CSV has correct headers and properly escaped values
- Export respects current search/vertical/delivery status filters
- Button shows loading state during export
- Button disabled when no leads to export
</success_criteria>

<output>
After completion, create `.planning/phases/33-export-analytics/33-01-SUMMARY.md`
</output>
