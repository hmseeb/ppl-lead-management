---
phase: 30-broker-reorder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/portal/orders-list.tsx
  - src/components/portal/order-form.tsx
  - src/app/portal/(protected)/orders/new/page.tsx
autonomous: true
requirements: [REORD-01, REORD-02, REORD-03, REORD-04]

must_haves:
  truths:
    - "Broker sees a Reorder button on each completed order in the portal orders list"
    - "Clicking Reorder opens the order form pre-filled with the original vertical, credit tier, and lead count"
    - "Submitting the pre-filled form redirects to Stripe Checkout with correct amount"
    - "New order only appears after Stripe payment succeeds (no change to existing webhook flow)"
  artifacts:
    - path: "src/components/portal/orders-list.tsx"
      provides: "Reorder button on completed order rows"
      contains: "Reorder"
    - path: "src/components/portal/order-form.tsx"
      provides: "Pre-fill support via props with defaults from URL params"
      contains: "defaultVertical"
    - path: "src/app/portal/(protected)/orders/new/page.tsx"
      provides: "Passes searchParams to OrderForm for pre-fill"
      contains: "searchParams"
  key_links:
    - from: "src/components/portal/orders-list.tsx"
      to: "/portal/orders/new?reorder_vertical=...&reorder_credit=...&reorder_count=..."
      via: "Link with query params from completed order data"
      pattern: "reorder_vertical"
    - from: "src/app/portal/(protected)/orders/new/page.tsx"
      to: "src/components/portal/order-form.tsx"
      via: "Props passing searchParams as default values"
      pattern: "defaultVertical|defaultCreditTier|defaultLeadCount"
    - from: "src/components/portal/order-form.tsx"
      to: "src/lib/actions/portal-order.ts createCheckoutSession"
      via: "Existing form submission (unchanged)"
      pattern: "createCheckoutSession"
---

<objective>
Add broker reorder flow: a "Reorder" button on completed orders that navigates to the order form pre-filled with the original order's parameters, then proceeds through the existing Stripe Checkout payment flow.

Purpose: Brokers can quickly repeat a completed order without re-entering all parameters, while still paying via Stripe (no free reorders for brokers).
Output: Modified orders-list.tsx with Reorder button, modified order-form.tsx with pre-fill support, modified new order page with searchParams passthrough.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/portal/orders-list.tsx
@src/components/portal/order-form.tsx
@src/app/portal/(protected)/orders/new/page.tsx
@src/lib/actions/portal-order.ts
@src/lib/schemas/portal-order.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Reorder button to completed orders and pre-fill support in OrderForm</name>
  <files>
    src/components/portal/orders-list.tsx
    src/components/portal/order-form.tsx
    src/app/portal/(protected)/orders/new/page.tsx
  </files>
  <action>
**orders-list.tsx** -- Add a "Reorder" button to the OrderRow component, visible only when `order.status === 'completed'`. The button is a Next.js Link (not a server action) that navigates to `/portal/orders/new` with query params encoding the original order's data:
- `reorder_vertical` = `order.verticals[0]` (first vertical)
- `reorder_credit` = `order.credit_score_min`
- `reorder_count` = `order.total_leads`

Place the Reorder button in the actions area (the `div` with `flex items-center gap-2`) alongside the existing Pause/Resume buttons. Use the `RefreshCw` icon from lucide-react, matching the existing button styling (`variant="outline" size="sm" className="h-7 text-xs"`). Import Link from next/link.

**orders/new/page.tsx** -- Convert from a simple function to accept `searchParams` prop (Next.js page convention). Extract `reorder_vertical`, `reorder_credit`, `reorder_count` from searchParams and pass them as props to OrderForm:
```
<OrderForm
  defaultVertical={searchParams?.reorder_vertical}
  defaultCreditTier={searchParams?.reorder_credit}
  defaultLeadCount={searchParams?.reorder_count}
/>
```
Also update the page title: if any reorder param is present, show "Reorder" instead of "Create Order". Keep the back link pointing to `/portal/orders` (not `/portal`) when reordering, so the broker returns to their orders list.

**order-form.tsx** -- Add optional props to the OrderForm component:
```typescript
type OrderFormProps = {
  defaultVertical?: string
  defaultCreditTier?: string
  defaultLeadCount?: string
}
```
Initialize the three useState hooks with these defaults instead of empty strings:
- `vertical` -> `props.defaultVertical || ''`
- `creditTierMin` -> `props.defaultCreditTier || ''`
- `leadCount` -> `props.defaultLeadCount || ''`

When defaults are provided (reorder mode), update the CardTitle to "Reorder Leads" and CardDescription to "Reordering with your previous settings. Adjust if needed, then proceed to payment." This tells the broker they can still modify values before paying.

No changes to createCheckoutSession or the Stripe webhook. The existing payment flow handles everything correctly: form submits -> Stripe Checkout session created -> redirect to Stripe -> webhook creates order on success. Abandoned checkouts create no order (REORD-04 satisfied by existing infrastructure).
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-leadr-mgmt && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>
      1. Visit /portal/orders -- completed orders should show a "Reorder" button
      2. Click Reorder -- should navigate to /portal/orders/new?reorder_vertical=...&reorder_credit=...&reorder_count=...
      3. Order form should be pre-filled with the original order's vertical, credit tier, and lead count
      4. Form title should say "Reorder Leads"
      5. Submitting should redirect to Stripe Checkout
      6. Abandoning checkout should create no order
      7. Completing payment should create the new order
    </manual>
  </verify>
  <done>
    - Completed orders in portal show a "Reorder" button (REORD-01)
    - Clicking Reorder navigates to pre-filled order form with original vertical, credit tier, lead count (REORD-02)
    - Submitting the form goes through Stripe Checkout payment flow (REORD-03)
    - Order only created after successful payment, not on form submit (REORD-04)
    - Active/paused orders do NOT show the Reorder button
    - Form values are editable (broker can adjust before paying)
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- no type errors
2. Navigate to /portal/orders with at least one completed order visible
3. Click "Reorder" on a completed order
4. Verify form pre-fills with correct vertical, credit tier, and lead count
5. Verify form title shows "Reorder Leads"
6. Submit and verify Stripe Checkout redirect with correct amount
7. Complete payment and verify new order appears in portal
8. Navigate directly to /portal/orders/new (no params) and verify normal "New Lead Order" form still works
</verification>

<success_criteria>
- Reorder button appears only on completed orders
- Pre-filled form shows correct values from original order
- Payment goes through Stripe Checkout (not free)
- New order created only after successful payment
- Normal order creation flow unaffected (no regression)
- Zero changes to admin dashboard code
- All files within portal directories only
</success_criteria>

<output>
After completion, create `.planning/phases/30-broker-reorder/30-01-SUMMARY.md`
</output>
